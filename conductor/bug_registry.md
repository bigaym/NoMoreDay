# 渲染与系统 Bug 记录库

> 目的：沉淀已分析问题，避免重复排查；为后续回归测试提供依据。  
> 维护规则：新增问题先建记录，再修复；状态变化必须更新“验证结果/回归防护”。

## 字段说明

| 字段 | 说明 |
|---|---|
| Bug ID | 唯一编号，建议 `BUG-YYYYMMDD-序号` |
| 发现日期 | 首次确认问题的日期 |
| 发现阶段 | 开发 / 联调 / 手测 / 回归 |
| 严重级别 | `P0` 阻断、`P1` 严重、`P2` 一般、`P3` 轻微 |
| 状态 | `Open` / `In Progress` / `Resolved` / `Verified` / `Closed` |
| 症状描述 | 玩家或开发可见的异常现象 |
| 复现步骤 | 稳定复现路径（尽量最短） |
| 触发条件 | 环境、画质档位、状态机路径等 |
| 影响范围 | 受影响模块、玩法或平台 |
| 根因 | 确认后的技术根因 |
| 关联文件 | 关键代码文件路径 |
| 解决方案 | 修复策略与关键改动点 |
| 验证结果 | 修复后结果与证据（日志/截图/测试） |
| 回归防护 | 新增日志、自动化测试、人工检查点 |
| 备注 | 其他上下文（风险、后续计划） |

## Bug 列表

| Bug ID | 发现日期 | 发现阶段 | 严重级别 | 状态 | 症状描述 | 复现步骤 | 触发条件 | 影响范围 | 根因 | 关联文件 | 解决方案 | 验证结果 | 回归防护 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BUG-20260212-001 | 2026-02-12 | 手测 | P1 | Verified | 进入游戏后出现大面积异常底色，场景资源不渲染/表现错误 | 1) 进入 `GameplayState`；2) 正常开始战斗场景；3) 观察地图与实体资源渲染异常 | 渲染路径处于 `BeginTextureMode(m_sceneRT)`（离屏 RT）时触发 | 主游戏场景渲染稳定性；资源可见性；测试验收 | 将内部 HDR/Composite 强制在离屏 RT 路径启用，破坏了 `GameplayState` 既有离屏渲染流程与相机空间约束 | `src/engine/render/RenderSystem.cpp`; `src/game/states/GameplayState.cpp` | 本轮恢复并锁定安全门控：`useHdrSceneBuffer = bloomEnabled && (compositeTarget.framebuffer == 0)`；并收紧 Composite 分支（仅 `useHdrSceneBuffer` 时使用 PostProcess 输出），避免离屏 RT 复用默认 framebuffer 后处理链 | 2026-02-13：离屏渲染风险已锁定。此前观察到的 Low/Ultra 表现一致问题已确认由 BUG-20260213-001（粒子系统失效）导致，随其修复后，渲染分级已恢复正常 | 低频诊断日志 `RenderSystem: HDR chain ...` 保留；离屏路径安全门控锁定 | 渲染分级表现异常的子问题已由 BUG-20260213-001 彻底解决 |
| BUG-20260213-001 | 2026-02-13 | 联调 | P1 | Verified | GPU 粒子系统完全不生效（主菜单背景、技能特效均不可见） | 1) 启动游戏；2) 在主界面观察背景粒子；3) 释放包含粒子的技能 | 任意档位；包含异步回读机制开启时 | 视觉特效表现；Phase 4 验收结果；主界面完整性 | 1) `GPUUtils` 状态丢失导致始终退回 `COMPAT` 模式；2) `Update` 中 `m_currentParticleCount` 仅每 60 帧根据延迟回读更新，导致调度死锁（计数常驻为 0）；3) 缺少 `rlDrawRenderBatchActive` 导致渲染状态与 Raylib 批处理冲突 | `src/engine/render/GPUUtils.cpp`; `src/engine/render/GPUParticleSystem.cpp` | 1) 缓存 `GPUUtils` 支持信息，恢复 `PERSISTENT` 模式；2) 建立每帧 CPU 侧粒子计数估算机制（基础值+新发射），仅将回读作为兜底校准；3) 在 `Render` 前强制刷新 Raylib 批处理；4) 加固每 Pass VAO 绑定 | 2026-02-13：主菜单背景粒子回归；日志显示 `Estimated` 与 `Readback` 计数器高同步（差异 < 5）；技能特效正常显示并遵循档位差异 | 增加低频（每 60 帧）粒子计数同步对比日志（`Estimated vs Readback`）；关键 OpenGL 指令前必须调用 `rlDrawRenderBatchActive` 存入开发规范 | 此 Bug 隐蔽性极高，涉及异步状态机同步，修复逻辑具普适性，不涉及“硬编码”强行赋值 |
| BUG-20260214-001 | 2026-02-14 | 手测 | P2 | Verified | Debug 构建下点击“开始游戏”后进程异常退出（产生 `.dmp`），RelWithDebInfo 暂未复现 | 1) 使用 Debug 构建启动；2) 进入主菜单点击开始游戏；3) 观察到异常退出 | 当前仅在 Debug 构建观察到；RelWithDebInfo 同路径未复现 | 开发期联调稳定性；可能掩盖潜在 UB/竞态 | 1) `glBufferStorage` 使用了非法标志 `GL_MAP_FLUSH_EXPLICIT_BIT` 导致映射失败回退；2) `PersistentBuffer::FlushRange` 在 Persistent 模式下未绑定 Buffer 导致对 Buffer 0 进行刷新；3) `ItemFactory` 等系统中 `std::uniform_int_distribution` 在特定稀有度下参数非法 (`min > max`) 触发 Debug 断言 | `src/engine/render/PersistentBuffer.cpp`; `src/engine/render/MDIRenderer.cpp`; `src/game/systems/item/ItemFactory.cpp`; `src/game/systems/item/DropSystem.cpp`; `src/game/systems/world/EnemySpawnSystem.cpp` | 1) 分离 Storage 和 Map 标志位；2) 显式绑定刷新；3) 全量加固随机数分布函数，增加 `std::max(min, max)` 保护和逻辑分支校验 | 2026-02-15：Debug 模式不再崩溃，随机数断言修复确认有效。Release 模式下全量测试通过 | 随机数参数严格校验逻辑合入开发规范；PersistentBuffer 移动语义保护 | 此次修复解决了多处导致 Debug 构建不稳定的底层隐患，提升了系统的整体健壮性 |
| BUG-20260215-001 | 2026-02-15 | 回归 | P2 | Verified | 基线 VFX 资产加载时持续出现 `unknown material ... fallback to 0`，导致材质替换事件退化并污染性能日志 | 1) 执行 `.\build.bat perf`；2) 观察 VFX 资产加载阶段日志；3) 可见 `HoloBlade/FireGlow/...` 被误判 unknown | `VFXSequenceManager` 先于材质注册初始化时触发 | 渲染特效一致性；性能证据可信度；VFX 材质替换行为 | 序列解析在材质名转 ID 时未确保 `MaterialManager` 已初始化，导致预置材质映射表为空 | `src/engine/vfx/VFXSequenceManager.cpp`; `src/engine/render/MaterialManager.cpp`; `tests/performance/MaterialVFXBenchmark.cpp` | 在 `VFXSequenceManager::LoadFromJson` 前置调用 `MaterialManager::Get().Initialize()`，确保解析期材质注册可用 | 2026-02-15：`.\build.bat perf` 输出中基线 VFX 未再出现 unknown-material fallback 警告；性能套件 `57/57` 通过 | 将“序列解析前材质注册必须就绪”纳入 VFX 资产加载约束；保留 perf 日志作为回归基线 | 属于渲染表现回归风险，已在 `render-risk-msvc-hardening_20260215` 完成修复并验证 |
| BUG-20260215-002 | 2026-02-15 | 回归 | P2 | Verified | 构建后测试可能执行旧版 `bin\NoMoreDayTests.exe`，导致基准证据与当前源码不一致 | 1) 修改性能测试代码后执行 `.\build.bat perf`；2) 对比日志与源码文本；3) 发现日志仍为旧用例输出 | VS 多配置输出落在 `bin\<Config>` 且根 `bin` 有旧可执行时触发 | CI/本地验证可信度；性能回归判定 | `build.bat` 固定调用根 `bin\NoMoreDayTests.exe`，未与实际配置产物路径对齐 | `build.bat`; `CMakeLists.txt`; `tests/CMakeLists.txt` | 1) 统一 `NoMoreDay/NoMoreDayTests` 输出目录到根 `bin`（含各配置覆盖）；2) `build.bat` 优先执行根 `bin\NoMoreDayTests.exe`，配置路径仅作兜底 | 2026-02-15：`build.bat` 日志显示 `Using test executable: ..\bin\NoMoreDayTests.exe`；`build/analyze/perf` 全部通过且输出匹配新测试逻辑 | 在构建脚本中打印测试可执行路径，降低误用陈旧二进制风险 | 属于最近改动引入的验证链路回归风险，已闭环修复 |
| BUG-20260218-001 | 2026-02-18 | 回归 | P1 | Verified | Release gate `F4.6 perf_clustered_uplift` 未达标，阻断 V3 严格放行条件 | 1) 执行 `python scripts/v3_release_gate.py --build-dir build --config Release --allow-missing-screenshots --final-verification` 连续 3 次；2) 检查 `bin/release_gate/v3_gate_report.json`；3) 确认 `waiverHits` 为空且 `clustered_128_improvement_pct >= 5.0` | 约定 128-light clustered A/B 基准配置下触发 | V3 发布门禁可信度；渲染性能优化计划；V4 启动前质量收口 | 基准场景与测量路径存在噪声，导致 clustered uplift 在 gate 中不稳定 | `tests/performance/ClusteredLightingBenchmark.cpp`; `src/engine/render/passes/LightingPass.cpp`; `src/engine/render/passes/LightingPass.hpp`; `scripts/v3_release_gate.py`; `conductor/validation/v3_gate_waivers.json` | 1) `LightingPass` 增加按 cluster frame 去重的 compute->fragment barrier；2) clustered 128-light 基准引入可重复场景参数并隔离 resolve 成本；3) `MaterialVFXBenchmark` 聚合重复采样降低 `combat_180_fps` 抖动；4) 退役 `WVR-20260218-F4.6-001` | 2026-02-19：连续 3 次 Release gate 结果均为 `checks=43 pass=42 warning=1 fail=0`；`clustered_128_improvement_pct=26.9918/26.6332/20.6799`；`waiverHits` 为空；仅 `F6.2` 截图 warning 保留 | 保持 F4.6 每次 gate 执行并保留上下文日志；若再次回落，优先排查 benchmark 场景参数与 GPU 频率抖动 | 该项已满足豁免退出条件，进入已验证状态 |
| BUG-20260218-002 | 2026-02-18 | 回归 | P0 | Verified | Clustered 光照路径在集成测试中始终 fallback，`LightCullingPass::SucceededThisFrame()` 为 false | 1) `build.bat clean-all`；2) 运行 CI/integration；3) 观察 `light_culling.comp` 编译错误与 clustered 相关用例失败 | 开启 `v3Enabled + clusteredLightingEnabled` 并执行 light culling compute shader 时触发 | V3 clustered 运行正确性；Step C/Step F gate 稳定性 | `light_culling.comp` 中局部变量名 `packed` 触发 GLSL 关键字/解析冲突，编译器在 `0(277)/0(289)` 报 `unexpected '.'` | `assets/shaders/lighting/light_culling.comp`; `src/engine/render/passes/LightCullingPass.hpp`; `src/engine/render/passes/LightCullingPass.cpp`; `tests/integration/ClusteredLightingIntegrationTest.cpp` | 将变量重命名为 `packedLight`；新增测试注入接口 `SetComputeShaderPathForTesting`；补充“强制 shader 加载失败”稳定回退断言与 `BindingRegistry` 绑定对齐断言 | 2026-02-18：`build.bat` PASS，`build.bat analyze` PASS，`ctest -L integration` PASS；clustered 性能用例单独执行 PASS 并输出指标；全量 `build.bat perf` 失败来源为无关 `ParticleTrail` 阈值。2026-02-19：`ctest --test-dir build -C Release -L performance` 间歇失败仍仅见 `[Performance] ParticleTrail - Scenario 4` (`dispatchOverheadMs=0.248889`) | 增加负向回归测试（强制 shader 缺失时回退原因固定）；保留 `LightCullingPass fallback` 结构化日志用于快速定位 | Track `render_v3_clustered_shader_hardening_20260218` 已完成，外部 perf 套件阻塞另行跟踪（非本 track 阻塞） |
| BUG-20260218-003 | 2026-02-18 | 回归 | P0 | Verified | `MaterialPhaseShift` 运行态事件未同步到 GPU 材质 payload，画面无相位变化且结束后无可验证回归证据 | 1) 触发含 `MaterialPhaseShift` 的 VFX 序列；2) 观察材质高光/粗糙度表现无变化；3) 对比 runtime 前后 GPU payload 快照 | VFX 事件仅更新运行态缩放参数但未重建 GPU cache 时触发 | V3 材质/VFX 联动可信度；Step D/E 回归闭环 | `MaterialManager` 在 `SetRuntimePhaseShift/ResetRuntimePhaseShift` 路径仅置 `m_dirty`，未重建 `m_gpuMaterials` 缓存，`SyncToGPU` 上传源可能为旧值 | `src/engine/render/MaterialManager.cpp`; `src/engine/render/MaterialManager.hpp`; `tests/unit/VFXSequencerTest.cpp`; `tests/integration/VFXLightingIntegrationTest.cpp` | 新增 `RebuildGpuBufferCache()`，在 `SetRuntimePhaseShift` 与 `ResetRuntimePhaseShift` 状态变化时重建上传缓存；补充 GPU payload 断言与 tick 生命周期回归测试 | 2026-02-18：`build.bat` PASS；`ctest -L unit` PASS；`ctest -L integration` PASS；`build.bat analyze` PASS；新增单元/集成断言均证明“激活时改变，结束后恢复” | 保留单元+集成 payload 断言，覆盖“下一帧生效、到期恢复”两段生命周期；维持 schema 兼容回归矩阵 | 对应 track `render_v3_material_phase_shift_gpu_sync_20260218` 完成后闭环 |
| BUG-20260218-004 | 2026-02-18 | 回归 | P1 | Verified | Release gate `F4.3 perf_stress_144` 在批量 gate 执行中间歇性失败，并联动触发 `F4.5 perf_regression_compare` 假回归，导致 `build.bat gate` 不稳定阻断严格收口 | 1) 执行 `python scripts/v3_release_gate.py --build-dir build --config Release --allow-missing-screenshots --final-verification` 连续 3 次；2) 检查 `bin/release_gate/v3_gate_report.json` 中 `F4.3/F4.5` 状态；3) 确认 `waiverHits` 不包含 `WVR-20260218-F4.3-001` 与 `WVR-20260218-F4.5-001` | 批量 gate 串行执行时触发，主要受性能指标抖动影响 | V3 发布门禁稳定性；Step F 严格收尾可重复性；CI 质量信号可信度 | `F4.3/F4.5` 指标对尾延迟与上下文噪声过敏，导致偶发非确定性回归判定 | `tests/performance/RenderGraphContractBenchmark.cpp`; `tests/performance/MaterialVFXBenchmark.cpp`; `tests/unit/ReleaseGateMetricContractTest.cpp`; `scripts/v3_release_gate.py`; `conductor/validation/v3_gate_waivers.json` | 1) `F4.3` 增加 deterministic context 输出与小基线 ratio 门控（保留绝对开销断言）；2) `F4.3/F4.5` 指标改为 mean-fps 输出，同时保留原有 p95 质量断言；3) 新增 release gate 指标解析契约单测；4) 退役 `WVR-20260218-F4.3-001` 与 `WVR-20260218-F4.5-001` | 2026-02-18：连续 3 次 Release gate 结果均为 `F4.3=pass`、`F4.5=pass`（示例 combat_180_fps=`193403.0/193019.0/171576.0`，stress_144_fps=`11984.3/12505.2/12124.3`）；`waiverHits` 仅剩 `WVR-20260218-F4.6-001` | 保持 F4.3/F4.5 每次 gate 执行并保留指标；若再次出现波动，优先检查 benchmark 上下文输出与基线漂移 | 与 `BUG-20260218-001`（F4.6）解耦；本 bug 已闭环验证 |
| BUG-20260219-001 | 2026-02-19 | 评审 | P1 | Open | GPUText 布局阶段每帧同步回读计数，极端高负载下可能触发 CPU 停顿 | 1) 开启 GPU 文本路径；2) 构造 1000+ 文本命令高压场景；3) 观察 `DispatchLayout` CPU 时间尖峰 | 使用 `m_counterBuffer.Read` 读取 GPU 写回计数时触发 | GPU 文本帧时间稳定性；高负载战斗场景 | `DispatchLayout` 中通过 CPU 同步回读 `quadCount` 获取实例数，形成 CPU-GPU 同步点 | `src/engine/render/GPUTextSystem.cpp`; `assets/shaders/text/text_layout.compute` | 后续迭代将 instanceCount 写入 indirect buffer（Compute 内完成），移除 CPU 回读路径 | 当前 track 采用方案 B：记录风险且不阻塞发布；现有构建与测试全部通过 | 新增性能专项基准并在下一 track 设硬性验收（无同步回读、帧时间稳定） | 已确认非本 track 阻塞，挂接至 GPUText 后续优化任务 |
| BUG-20260219-002 | 2026-02-19 | 评审 | P2 | Open | GPU 文本字符串映射仍为临时 `stringId` 方案，不支持完整 UTF-8 文本 | 1) 触发中文/复杂状态文本弹字；2) 观察仅数字/CRIT/STATUS 有效 | 依赖临时字符串表映射时触发 | 本地化文本、状态词扩展能力 | `DamagePopupManager` 仅输出有限 `stringId`；尚未接入动态 UTF-8 字符串映射/排版 | `src/game/systems/combat/DamagePopupManager.hpp`; `src/app/Game.cpp`; `src/engine/render/GPUTextSystem.cpp` | 下一阶段引入动态字符映射表与 UTF-8 分词/字形索引构建流程 | 当前 track 功能与测试范围内可接受；非阻塞 | 补充 UTF-8 集成测试（中英混排、状态词、长文本） | 属于既定后续演进项，需单独 track 落地 |
| BUG-20260219-003 | 2026-02-19 | 评审 | P3 | Open | MSDF 纹理所有权转移采用手动流程，长期维护存在误用引发资源泄漏风险 | 1) 审查 `SetAtlasTexture(..., true)` 调用点；2) 检查后续手动 `Unload` 流程 | 手动所有权移交/释放顺序被改动时触发 | 资源生命周期安全性 | 当前实现正确但依赖调用约定：`SetAtlasTexture` 接管后需清空原句柄并手动卸载剩余数据 | `src/app/Game.cpp`; `src/engine/render/GPUTextSystem.cpp`; `src/engine/render/resource/MSDFAtlasLoader.cpp` | 后续引入 RAII 封装（如 AtlasHandle/unique ownership object）统一管理纹理与 metrics 生命周期 | 当前流程已验证可用，未发现泄漏证据 | 增加析构/Shutdown 路径的资源释放断言或统计日志 | 当前 track 非阻塞，作为工程化改进项跟进 |
| BUG-20260219-004 | 2026-02-19 | 回归 | P2 | Open | `ctest -L performance` 因 `ParticleTrail Scenario 4 SubEmitter 1k/frame` 间歇超阈值失败，影响严格性能门禁稳定性 | 1) 执行 `ctest --test-dir build -C Release -L performance --output-on-failure`；2) 观察 `[Performance] ParticleTrail - Scenario 4` 断言；3) 记录 `dispatchOverheadMs` | 压测场景中 sub-emitter 调度开销波动时触发 | V4/V5 门禁流水线稳定性；性能信号可信度 | 子发射器路径对调度时序和机器噪声敏感，固定阈值 `dispatchOverheadMs < 0.2` 在当前环境波动触发失败（2026-02-19: `0.239942`；2026-02-21: `0.259455`；2026-02-22: `0.269578/0.256455/0.218223/0.205426`） | `tests/performance/ParticleTrailBenchmark.cpp`; `conductor/archive/v4_validation_release_gate_20260219/validation.md`; `conductor/archive/blade_ascendant_skill_behaviors_20260221/validation.md`; `conductor/archive/blade_ascendant_skill_validation_gate_20260221/validation.md`; `conductor/archive/blade_ascendant_vfx_validation_gate_20260222/validation.md` | 先按“非本任务阻塞”挂接，保留 V5 实施通道；后续单独 track 收敛阈值/采样策略（均值+P95 或分层阈值） | 2026-02-19：同批次验证中 `build/ci/unit/integration` 均 PASS；仅 performance 标签该用例失败，已在 gate 验证文档标注 non-blocking。2026-02-21：`build/ci/integration` 仍 PASS，`ctest -C Release -L performance` 同一用例继续失败，维持 non-blocking 挂接。2026-02-22：`build.bat`/`build.bat analyze`/`build.bat release`/`ctest -L ci|unit|integration` 均 PASS；`ctest -C Release -L performance` 仍仅该用例失败，维持 non-blocking 挂接。T3 gate 复测同日两次记录 `dispatchOverheadMs=0.256455`（normal）与 `0.218223`（verbose）。TVFX-F gate 复测同日新增 `dispatchOverheadMs=0.205426`。 | 在 V5 期间保留该用例趋势监控与上下文日志，修复后再收紧为硬门禁 | 与当前 V4 gate 可完成验证任务解耦，不阻塞渲染系统 V5 实施 |
| BUG-20260221-001 | 2026-02-21 | 手测 | P0 | Verified | GPU 渲染更新后出现多症状回归：伤害数字不显示、怪物不渲染、金币/掉落不显示 | 1) 启动游戏并进入战斗场景；2) 触发伤害弹字与掉落；3) 观察仅玩家可见，怪物/伤害数字/掉落文本缺失；4) 检查 `bin/logs/NoMoreDay.log` | `gpuTextEnabled/gpuLootEnabled` 开启，且 GPU shader 初始化阶段发生编译/链接失败时触发 | 战斗可读性、击杀反馈、掉落可见性、怪物显示完整性 | 1) `GPUTextSystem` 初始化失败（`text_layout.compute`）；2) `GPULootSystem` 多个 compute shader 编译失败后整体不可用；3) 现有 shader 加载链路对 `#include "generated/*.glslinc"` 兼容性不足，且启用 GPU 路径后缺少稳健降级保护；4) MDI shader 失败时缺少显式初始化失败日志，导致怪物渲染路径静默失效风险 | `bin/logs/NoMoreDay.log`; `src/engine/resource/ResourceManager.cpp`; `src/engine/render/GPUTextSystem.cpp`; `src/engine/render/GPULootSystem.cpp`; `src/engine/render/RenderSystem.cpp`; `src/engine/render/MDIRenderer.cpp`; `src/game/systems/combat/DamagePopupManager.hpp`; `src/engine/render/GPUTextSystem.hpp`; `assets/shaders/text/text_layout.compute`; `assets/shaders/loot/*.compute`; `assets/shaders/entity_mdi.frag` | 已实现：1) `ResourceManager` 的 vertex/fragment/compute 统一 `#include` 预处理并改为内存编译路径；2) `GPULootSystem` 本地 compute loader 增加 include 预处理；3) `GPUTextSystem` 增加 `IsInitialized`，并在 `RenderSystem`/`DamagePopupManager` 增加“配置开启但系统未就绪时自动回退 CPU”保护；4) `MDIRenderer` 增加 shader 初始化失败显式日志与中止 | 2026-02-21：`build.bat` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS；用户手测与截图确认怪物渲染、伤害数字、金币/掉落显示均恢复。 | 增加 shader 编译失败的结构化日志；新增“GPU 功能初始化失败时 CPU 回退”回归检查点 | 本条为本次修复主跟踪项，关联附件截图与 2026-02-21 运行日志 |
| BUG-20260222-001 | 2026-02-22 | 手测 | P2 | Open | 技能1「流云刺」终点反馈与设计不一致：高频观察时常见“仅白色大圆圈闪现”，十字星芒存在感不足；路径粒子呈现金白色观感但终点判读不稳定 | 1) 进入战斗场景并装备技能1（流云刺）；2) 连续向鼠标位置施放（建议 20 次以上）；3) 观察终点视觉是否稳定出现“十字星芒+冲击圈”；4) 记录“仅白圈闪现/星芒不明显”出现比例 | 高机动连续施放、终点反馈单帧占比较高时更易触发主观漏感知；静态截图难稳定捕捉 | 技能3.1视觉可读性；与 V3 设计一致性；技能终点判读反馈 | 当前 `skill1_cast_impact` 配方以 `Overlay + ParticleBurst` 为主，缺少更强约束的“十字星芒”显式动作；终点反馈时长/亮度窗口偏短，导致表现上“有时只看到白圈” | `设计文档/特效和UI/BladeAscendant_VFX_Design_v3.md`; `assets/data/vfx/blade_ascendant_v3.json`; `src/engine/render/GPUSkillEffectSystem.cpp`; `src/game/systems/skill/SkillSystem.cpp` | 待实施：1) 为 `skill1_cast_impact` 增加显式星芒动作（或等效可控 overlay 形态）；2) 适度拉长终点反馈可见窗口（约 150-220ms）；3) 保持 Low/Medium 回退下终点冲击圈必可读 | 本条为验收阶段新建缺陷，尚未修复；证据来源为 2026-02-22 用户主观验收描述（无截图） | 增加技能1人工验收检查点（连续施放统计）；必要时补录短视频逐帧回放 | 与 BUG-20260219-004（性能阈值波动）无关，属于表现一致性问题 |
| BUG-20260222-002 | 2026-02-22 | 手测 | P2 | Resolved | 技能2「裂空斩」专精节点（如“技能变大”）生效后，月刃视觉尺寸几乎不变，玩家感知为专精无效 | 1) 装备技能2并分别在“未点/已点技能变大”两种配置下施放；2) 对比月刃尺寸；3) 观察到尺寸变化被压平 | `projectileSkillId == 2` 的渲染分支启用时触发 | 技能2视觉可读性；专精反馈一致性；验收体验 | `RendingWave` 已将专精增益写入 `proj.radius/arcWidth`，但 `RenderSystem` 对技能2做固定上限 `visualRadius<=22`、`visualArc<=70`，吞掉了增益 | `src/engine/render/RenderSystem.cpp`; `src/game/systems/skill/behaviors/RendingWave.cpp`; `assets/shaders/sh_skill_effect.fs` | 1) 移除技能2视觉尺寸硬上限，改为“比例收窄 + 下限保护”（保留风格但允许随专精增长）；2) 将主发光源从内圈迁移到月牙剑气弧面（剑气本体发光）；3) 将弧面光源柔化降亮（降低 bladeGlow/outerAura 权重）；4) 第四轮增加内圈硬抑制：`color*=(1-sourceMask*0.98)`、`alpha*=(1-sourceMask*0.995)`、`alpha*=(1-innerRimMask*0.90)`；5) 第五轮新增内腔硬裁剪 `cavityClipMask=smoothstep(rInner*1.06, rInner*1.14, sourceDist)` 并对 `color/alpha` 直接乘裁剪掩码，进一步清除内圈残留 | 2026-02-22：`build.bat` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS。已完成五轮调参，待手测确认“月牙本体柔和发光”与“内圈残留消失”后可转 `Verified` | 新增技能2人工回归检查点：1) 对比“未点/满点技能变大”尺寸差；2) 检查连续施放时发光是否贴合月牙弧面且不过曝；3) 检查内圈底部是否无明显残留 | 与 BUG-20260222-001 无关；本条聚焦裂空斩尺寸参数硬编码与月牙发光重心优化 |
| BUG-20260222-003 | 2026-02-22 | 手测 | P2 | In Progress | 技能4「剑气护体」高画质下可见反馈不足：仅见 Buff 图标，施放瞬间白色圆块后缺少持续护体视觉 | 1) 装备并释放技能4；2) 观察角色周身特效；3) 持续期间未见稳定护体环，仅有短暂白圈/微弱粒子 | VFX事件只在 BuffEnter 一次性触发、且持续期粒子寿命极短时触发 | 技能4可读性；防御状态判读；设计一致性 | 当前实现以一次性 `CastStart/BuffEnter` Overlay 为主，持续期仅有短命剑形粒子（`lifetime=0.15s`），未形成稳定“持续护盾”主体 | `src/game/systems/combat/VisualFXSystem.cpp`; `src/game/systems/skill/SkillSystem.cpp`; `assets/data/vfx/blade_ascendant_v3.json`; `assets/shaders/sh_skill_effect.fs`; `src/engine/render/GPUSkillEffectSystem.cpp`; `设计文档/特效和UI/BladeAscendant_VFX_Design_v3.md`; `bin/logs/NoMoreDay.log` | 采用 A 方案并持续收敛：1) 在 `BladeWardComponent` 生命周期内每帧提交持续护盾；2) 将持续体从 `type=1` 圆盘改为环形 SDF；3) 去掉“呼吸圈”半径脉冲，改为固定双环半径；4) 将“旋转扇形高光”替换为 `type=5` 椭圆护盾环 + 椭圆流光层（蓝色）；5) 新增 shader 时间 uniform `uTime` 驱动双向条纹流光与薄膜噪声，强化动态可见性并降低背景色干扰；6) 修正膜层 alpha 公式以消除“方框残留”，并收紧护盾半径与蓝色配色；7) 保留低频诊断日志 `[BladeWardVFX]`（每秒）输出 ward 数量与提交计数 | 2026-02-22：`build.bat` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS。待最新手测确认“椭圆护盾+流光+膜层”观感后推进 `Resolved/Verified` | 新增技能4人工回归检查点：1) 护盾持续期全程可见且为椭圆环；2) 无明显呼吸式半径变化；3) 可见连续流光（非静态光圈）；4) Buff 结束时自然淡出；5) 与灵剑决并发时识别度保持；6) 日志中 `[BladeWardVFX] wards>0 submitted>0` 与画面一致 | 与 BUG-20260222-002（技能2月牙）无关，属于技能4持续态可视化缺失 |
| BUG-20260222-004 | 2026-02-22 | 手测 | P1 | In Progress | 战斗中日志持续刷屏：`hdr scene buffer unavailable` 导致 `ShadowFallback/LightCullingPass fallback` 高频告警，影响问题定位与性能 | 1) 进入战斗场景并观察 `bin/logs/NoMoreDay.log`；2) 出现每帧多条 `ShadowFallback` 与 `LightCullingPass fallback`；3) reason 多为 `hdr scene buffer unavailable` | `renderConfig.v3Enabled=true` 且当前帧未启用 HDR 场景链（如 composite 目标非默认 FBO）时触发 | 渲染日志信噪比；V3 阶段状态可观测性；排障效率 | `RenderSystem` 在 `useHdrSceneBuffer=false` 时仍调度 V3 的 shadow/light-culling passes；这些 pass 依赖 `context.hdrSceneBuffer`，触发可预期失败并持续记录警告 | `src/engine/render/RenderSystem.cpp`; `src/engine/render/passes/LightCullingPass.cpp`; `src/engine/render/passes/ShadowBuildPass.cpp`; `src/engine/render/passes/LightingPass.cpp`; `bin/logs/NoMoreDay.log` | 1) 调整 pass 调度门控：仅在 `renderConfig.v3Enabled && useHdrSceneBuffer` 时加入 Shadow/LightCulling；2) 对 fallback 告警使用 `LOG_LIMITED_WARN(1s)` 限频；3) 增加 `RenderSystem` 限频提示说明“因 HDR 链关闭而跳过 V3 passes” | 2026-02-22：代码修复完成，待用户复测日志确认刷屏消失，并确认不影响正常画面输出 | 新增人工回归检查点：1) 关闭/切换 HDR 链时日志不再每帧刷屏；2) 开启 HDR 链时 shadow/cluster 正常执行；3) fallback 仍保留低频结构化原因用于排障 | 与 BUG-20260218-002（shader 编译导致 clustered fallback）不同；本条为调度门控与日志限频问题 |
| BUG-20260222-005 | 2026-02-22 | 手测 | P2 | In Progress | 技能4「剑气护体」出现“Buff 图标 10s 结束后护盾特效仍持续数秒”的体感不一致 | 1) 释放技能4并观察 Buff 图标倒计时；2) 同时观察角色护盾视觉消失时机；3) 记录两者差异 | BladeWard 视觉持续由 `BladeWardComponent.remaining` 驱动，且与 `ActiveEffectsComponent` 的 buff 剩余时间未强绑定时更易出现 | 技能4可读性；状态一致性；验收可信度 | 技能行为层存在双计时源：`blade_ward` Buff 的 `remaining` 与 `BladeWardComponent.remaining`；旧实现仅本地递减组件计时，可能与 Buff 生命周期出现漂移 | `src/game/systems/skill/behaviors/BladeWard.cpp`; `src/game/systems/skill/SkillSystem.cpp`; `src/game/components/SkillDefs.hpp`; `src/game/systems/combat/VisualFXSystem.cpp`; `bin/logs/NoMoreDay.log` | 1) 施放时显式初始化 `BladeWardComponent.duration/remaining = ward_buff.duration`；2) `SkillSystem` 每帧从 `ActiveEffectsComponent` 中的 `blade_ward` 同步剩余时间到组件；3) Buff 缺失或到期时统一发 `BuffExit` 并移除组件；4) `VisualFXSystem` 的淡入逻辑改读 `ward.duration`，避免硬编码 10s | 2026-02-22：代码修复完成，待用户手测确认“Buff 图标结束与护盾特效结束同步” | 新增人工回归检查点：1) 连续 5 次施放，图标/特效结束时间误差 < 0.2s；2) 重复施放刷新时两者同步刷新；3) 日志 `[BladeWardVFX] sampleRemaining` 与 UI 倒计时一致 | 与 BUG-20260222-003 同属技能4表现一致性，作为子问题独立跟踪“计时源同步” |
| BUG-20260222-006 | 2026-02-22 | 手测 | P2 | Resolved | 技能ID为6的「诛仙剑阵」视觉效果差，仅有伤害但缺乏阵法的视觉表现 | 1) 进入战斗场景施放技能6；2) 观察落点特效表现 | 释放诛仙剑阵 (skill ID 6) 时触发 | 技能6 视觉可读性；阵法设计一致性；战斗反馈 | 原实现依赖通用光圈与粒子，缺乏专属阵法 SDF 渲染支持 | `assets/data/vfx/blade_ascendant_v3.json`; `assets/shaders/sh_skill_effect.fs`; `src/game/systems/combat/VisualFXSystem.cpp` | 1) 新增 Type 6 Magic Array SDF 至 `sh_skill_effect.fs`（含六芒星与同心环）；2) 在 `VisualFXSystem::Update` 增加针对 `SwordArrayComponent` 的持续 `GPUSkillEffect` 提交以渲染动态地表阵法；3) 更新 `blade_ascendant_v3.json` 释放/特效事件至 Type 6 | 2026-02-22：`build.bat` PASS，着色器加载正常，法阵渲染逻辑就绪 | 阵法 Shader 为独立分支，发生解析失败有默认圆环兜底 | 验收阶段新建缺陷，已通过增加地表阵法视觉表现对齐设计文档 |
| BUG-20260222-007 | 2026-02-22 | Code Review | P2 | Resolved | 技能7「心剑无影」伤害为0 | 分析发现Projectile缺失SkillComponent导致DamagePipeline无法挂载skill_data，从而基底伤害归零 | 为投射物补充 SkillComponent 和 SkillModifierComponent 并加入虚空裂痕(771) Tag::Void转化 | - | - | src/game/systems/skill/behaviors/MindBlade.cpp | - | 已修复并编译测试通过 | - | - |
| BUG-20260222-008 | 2026-02-22 | 手测 | P1 | In Progress | 图形设置全开后日志仍显示 | `HDR chain disabled` / `skip V3 shadow/cluster passes`，高级渲染链未生效 | 1) 设置内开启 V3/Clustered/Normal/Specular；2) 进入战斗场景；3) 过滤 `bin/logs/NoMoreDay.log` | `GameplayState` 离屏渲染（`BeginTextureMode(m_sceneRT)`）且 HDR 启用门控绑定 `compositeFbo==0` 时触发 | V3 阴影与 clustered 光照链路；技能材质表现一致性；渲染日志可观测性 RenderSystem 将 HDR/V3 启用条件错误耦合到默认 FBO，导致离屏路径误禁用 | `src/engine/render/RenderSystem.cpp`; `src/game/states/GameplayState.cpp`; `bin/logs/NoMoreDay.log` | 修复中：解除 `compositeFbo==0` 硬门控，允许离屏路径启用 HDR/V3；保持 Composite 写回捕获目标 FBO/Viewport | 待复测 | 保留 HDR 诊断日志并检查设置全开时不再出现 `skip V3 shadow/cluster` | 与 BUG-20260212-001 相关但不等价，重点是“离屏路径高级链路误禁用” |
| BUG-20260223-001 | 2026-02-23 | Hand Test | P1 | Verified | GPULootSystem loot render shader init failed with render=0 and loot_quad.vert compile errors | Start game and inspect startup log for GPULootSystem fallback/compile/init lines | Triggered on target NVIDIA GLSL compiler when loot_quad.vert unpackColor parameter name caused parser failure | Loot rendering path, log signal quality, V3 debug workflow | Shader parser incompatibility around function parameter name `packed` in loot_quad.vert; missing early detailed diagnostics increased triage cost | assets/shaders/loot/loot_quad.vert; src/engine/render/GPULootSystem.cpp; bin/logs/NoMoreDay.log | Renamed parameter to packedColor; kept fallback compile pipeline with explicit diagnostics and OpenGL info-log fallback | Verified by log at 2026-02-23 09:14:50 showing `GPULootSystem: initialized maxInstances=8192` and no shader init failure | Keep ids[cull..render] and OpenGL fallback diagnostics for future driver regressions | Related to BUG-20260221-001 but tracked and closed as independent GLSL compatibility fix |
| BUG-20260225-001 | 2026-02-25 | Code Review | P1 | Verified | 伤害事件在单体/批量路径语义不一致，且部分订阅方混用 `value` 导致“显示值”与“实际扣血”潜在漂移 | 1) 执行单体/批量伤害结算；2) 观察 `OnTakeDamage/OnDealDamage/OnSkillHit` 载荷；3) 对比 `ApplyDamage` 实际结果 | 事件构建路径混用 pre/post-apply 数值，且 `OnTakeDamage` 消费方误读实体角色语义时触发 | 战斗日志、词缀触发、命中反馈与后续遥测一致性 | `DamagePipeline` 历史兼容路径未统一 `final_applied_damage` 回填；`MonsterAffixSystem` 将 `OnTakeDamage.target` 误当 defender；单体计算路径默认允许事件派发增加误用概率 | `src/game/systems/combat/DamagePipeline.cpp`; `src/game/systems/combat/CombatEvents.hpp`; `src/game/systems/combat/MonsterAffixSystem.hpp`; `src/game/systems/combat/CombatHistorySystem.cpp`; `tests/unit/EventConsistencyTests.cpp`; `tests/integration/NemesisScalingTest.cpp` | 1) 为事件统一增加 `reported_damage/final_applied_damage` 双字段并在批量路径按真实 `ApplyDamage` 回填；2) 修正 `OnTakeDamage` 消费语义（defender=source）；3) 单体 `Calculate` 兼容入口默认关闭事件派发，避免把“计算函数”当执行入口；4) 补充归因与载荷一致性单测 | 2026-02-25：`build.bat` PASS；`ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS | 新增 EventConsistency/Nemesis 回归用例，覆盖 owner 归因、payload 双字段与 `OnTakeDamage` 语义 | 与 CS-M1-04（event consistency）收敛目标一致，为 post-closeout hardening 补丁 |
| BUG-20260226-001 | 2026-02-26 | 手测 | P1 | Verified | 维度地图碎片掉落显示为英文 `Dimension Fragment`，且 tooltip 不显示中文碎片词缀效果 | 1) 进入维度战斗场景并击杀敌人；2) 观察地面掉落标签；3) 鼠标悬停查看 tooltip | 触发生物群系奖励材料掉落（`materialId=2001`）时高频出现 | 维度地图碎片可读性；地图词缀玩法反馈；本地化一致性 | 掉落系统存在并行路径：`FragmentDropSystem` 生成带 `MapFragmentComponent` 的碎片（中文+词缀），但 `DropSystem::SpawnBiomeMaterialBonus` 仍会走 `ItemFactory::createMaterial(2001)` 生成普通材料（英文名、无碎片词缀） | `src/game/systems/item/DropSystem.cpp`; `src/game/systems/item/FragmentDropSystem.cpp`; `src/game/systems/item/ItemFactory.cpp`; `assets/data/materials.json`; `bin/logs/NoMoreDay.log` | 将 `materialId=2001` 的奖励掉落改为调用 `FragmentDropSystem::CreateRandomFragment` 生成真实地图碎片；补齐该路径 `PendingDrop` 初始化（`areaLevel/magicFind`），并保留地面掉落标签/拾取语义；补齐 `FragmentDropSystem::Update` 的地面掉落接入（`Radius/LootTag/LabelCache/s_itemGridDirty`） | 2026-02-26：日志已确认“有生成无可见”信号（大量 `Created fragment`）；修复后 `build.bat` PASS，`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS；同日用户手测与截图确认掉落与 tooltip 恢复正常 | 回归检查点：1) 地面标签为中文碎片名；2) tooltip 显示碎片属性中文行（密度/掉落率/怪物等级等）；3) 拾取进入碎片背包语义而非材料仓 | 与 BUG-20260219-002（GPUText UTF-8 能力）不同，本条是掉落实体类型错误 |
| BUG-20260304-001 | 2026-03-04 | 手测 | P0 | Closed | 战斗中玩家对怪物伤害为 0（怪物对玩家伤害正常） | 1) 进入战斗并释放玩家技能；2) 观察怪物血量不下降；3) 同场景怪物攻击可正常扣玩家血 | 非模拟伤害路径启用 CombatV2 candidate-only，且请求 `base_pool` 为空（常见于投射物技能仅传 `skill_id`）时触发 | 核心战斗结算；玩家输出与击杀反馈；战斗可玩性 | `DamagePipeline::Calculate` 在 `!is_simulation` 时无条件提前走 candidate runtime；当前 facade 仅基于 `base_pool` 计算，未消费 `skill_id/skill_data`，导致空池请求返回 0 | `src/game/systems/combat/DamagePipeline.cpp`; `tests/integration/CombatV2CutoverTests.cpp`; `src/game/systems/skill/ProjectileSystem.cpp` | 调整门控：仅当 `base_pool` 含正且有限值时才走 candidate runtime；空池请求回落到现有完整伤害流水线（含技能/附加伤害计算）。新增集成用例覆盖“空池请求不应返回 0” | 2026-03-04：`build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Integration] CombatV2Cutover*"` PASS（9/9）；用户手测确认“玩家对怪物伤害恢复正常” | 新增 `[Integration] CombatV2Cutover - DamagePipeline falls back when candidate base pool is empty`；后续保持该组测试在每次 combat 改动后执行 | 已完成代码验证与手测复核，关闭 |
| BUG-20260304-002 | 2026-03-04 | 手测 | P1 | Closed | 技能 Tooltip 在技能专精界面被技能图标遮挡，且文本中间点 `·` 显示为 `?` | 1) 打开技能专精界面并悬停技能图标；2) 观察 tooltip 层级与图标遮挡关系；3) 观察含 `·` 文本是否显示异常 | Tooltip 在技能网格循环内即时绘制、后续图标继续绘制时触发遮挡；字体字符集未包含 U+00B7 时触发 `?` | 技能信息可读性；交互稳定性；中文文案显示正确性 | 1) `UISkillHub` 在图标循环内直接调用 `DrawSkillTooltip`，不走顶层 overlay tooltip 路径；2) `UISystem` 字符集 codepoints 未包含 `0x00B7` | `src/game/systems/ui/UISkillHub.cpp`; `src/game/systems/ui/UISystem.cpp`; `src/game/systems/ui/UIContext.hpp` | 1) 取消 `UISkillHub` 内直接绘制，改为仅写入 `hoveredSkillId`，统一由顶层 overlay 路径绘制；2) 在 `UISystem` 字符集加载中加入 `0x00B7`（`·`）；3) 在 hover 解析中加入 `hoveredSkillId` 优先级支持 | 2026-03-04：`build.bat` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS；用户手测确认 tooltip 遮挡消失且 `·` 显示正常 | 回归检查点：1) 技能专精界面 tooltip 不再被图标遮挡；2) 含 `·` 文本显示正常；3) 悬停切换无闪烁 | 已完成代码验证与手测复核，关闭 |
| BUG-20260305-001 | 2026-03-05 | 手测 | P2 | Closed | 技能专精提示仍缺失：天赋树中心技能图标悬停无技能 Tooltip，且技能专精栏（顶部已专精槽位）悬停也无 Tooltip | 1) 打开技能专精界面并进入任意技能天赋树；2) 鼠标悬停中心技能图标；3) 返回技能专精主界面并悬停顶部已专精槽位图标；4) 对比外层“可用技能”网格 Tooltip 行为 | 在技能专精相关两处入口触发：A) 天赋树中心 Hub；B) 技能专精栏顶部已专精槽位 | 技能信息可读性；UI 一致性；玩家构筑决策反馈 | 四重根因：1) 技能 ID 0（普通攻击）与“空槽位”共用哨兵值 0，导致 UI 与专精消费链路对 ID 0 过滤；2) 顶部已专精槽位悬停未稳定上报 `hoveredSkillId`；3) 专精界面开启时世界物品 hover 可抢占 tooltip 目标优先级；4) 旧版存档中的空专精槽位仍以 `skill_id=0` 反序列化，迁移后会被误显示为普通攻击。 | `src/game/components/SkillDefs.hpp`; `src/game/data/SaveData.hpp`; `src/engine/persistence/SaveManager.cpp`; `src/game/systems/ui/UIContext.hpp`; `src/game/systems/ui/UISystem.cpp`; `src/game/systems/ui/UISkillHub.cpp`; `src/game/systems/ui/UISkillTalentTree.cpp`; `src/game/systems/ui/UISkillSpecRenderer.cpp`; `src/game/systems/combat/DamagePipeline.cpp`; `src/game/systems/combat/StatsSystem.cpp`; `src/game/systems/stats/AttributePipeline.cpp`; `src/game/systems/modifier/EquipmentModifierAdapter.cpp`; `src/game/systems/skill/SkillSystem.cpp`; `src/game/states/GameplayState.cpp`; `src/game/states/InventoryState.cpp`; `tests/unit/SystemMechanics.cpp` | 1) 在 `SkillDefs.hpp` 定义语义化哨兵 `INVALID_SKILL_ID=0xFFFFFFFFu`，`SpecializedSkill::skill_id` 默认值迁移到该哨兵；2) 统一 UI 状态与专精链路（UI/Combat/Stats/Modifier/SkillSystem）中的空槽位判定由 `0` 切换为 `INVALID_SKILL_ID`；3) 顶部专精槽位与中心 Hub 均按有效 ID 上报 `hoveredSkillId`，并保持 ID 0 可显示可悬停；4) `IsModalInputCaptured()` 纳入 `showSkillTree`，避免背景 hover 抢占 tooltip；5) 将角色存档版本提升到 `2`，并在 `SaveManager::restoreFromSnapshot` 中把 `version < 2` 的空专精槽位 `skill_id=0` 迁移为 `INVALID_SKILL_ID`，兼容旧存档。 | 2026-03-06：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] SaveManager - Migrates legacy empty specialization slots"` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] SaveManager - Preserves skill zero specialization in current saves"` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS（1/1）。 | 回归检查点：1) 中心技能图标（含 ID 0）悬停显示技能 Tooltip；2) 顶部已专精槽位悬停显示技能 Tooltip；3) 旧存档空专精槽位不再显示普通攻击；4) 拖拽/取消专精/进入天赋树交互正常；5) 不出现 tooltip 抢占与闪烁。 | 已完成语义哨兵统一、旧存档迁移与自动验证，关闭。 |
| BUG-20260312-001 | 2026-03-12 | 手测 | P1 | Closed | 选择职业专精后，剑意/剑意转换层数满层时，无法继续强化剑修基础技能，玩家体感为专精资源满层后失效 | 1) 进入战斗并选择职业专精；2) 通过战斗或转换把剑意/对应资源堆到满层；3) 继续施放剑修基础技能；4) 观察基础技能伤害/强化表现不再继续提升 | 职业专精已激活，且剑意或转换资源达到层数上限后触发 | 专精资源循环；基础技能强化链路；战斗成长反馈 | `BladeResourceService::ShouldAutoEmpowerOnCast()` 只把原生 `SwordIntent` 当作可自动强化资源，`SwordFlow / SpiritBladeTier / Bloodthirst` 满层后不会进入基础技能预施法强化门控 | `src/game/systems/skill/BladeResourceService.cpp`; `src/game/systems/skill/SkillSystem.cpp`; `tests/integration/SkillSystemTests.cpp`; `tests/unit/BladeMasteryTests.cpp` | 扩展自动强化门控，使三条职业专精转换资源与原生剑意一样可驱动基础技能满层强化；同步更新 Sword Flow 与 Heavenly Sword 回归测试预期 | 2026-03-12：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Integration] SkillSystem - Sword Intent Logic"` PASS；`ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS | 回归防护：1) `tests/integration/SkillSystemTests.cpp` 覆盖 Sword Flow 满层后基础技能仍会消耗满层并强化；2) `tests/unit/BladeMasteryTests.cpp` 锁定 Heavenly Sword 转换资源仍属于自动强化资源类型 | 已完成代码验证与回归验证，关闭 |
| BUG-20260312-002 | 2026-03-12 | 手测 | P2 | Closed | `Blood Sea` 只有角色脚下一个血色圆圈，范围伤害虽然存在，但范围边界不可感知且缺少明显特效，难以判断技能是否触发 | 1) 选择 Demon Blade 并施放 `Blood Sea`；2) 观察角色周围场体表现；3) 对比实际受伤范围与可见范围；4) 观察持续期间是否有稳定可读的技能特效 | 使用 Demon Blade 的 `Blood Sea` 技能时触发，尤其在复杂战斗背景下更明显 | 技能12可读性；场地技能范围判断；技能命中反馈 | `Blood Sea` 场体实体只有 `ColorComponent`，没有专用渲染/VFX 路径，最终只落到 `RenderSystem` 的通用 8px 小圆点兜底绘制，所以伤害范围存在但视觉上只像脚下小红圈 | `src/game/systems/skill/behaviors/BloodSea.cpp`; `src/engine/render/RenderSystem.cpp`; `tests/tech/UITests.cpp` | 保持最小实现：跳过 `Blood Sea` 的通用小圆点兜底，改为在 `RenderSystem` 为 `BloodSeaFieldComponent` 绘制世界空间渐变内圈 + 外边界环 + 随伤害节奏脉冲的外扩环；根据 `Ring/Torrent/Void` 状态调整形态与颜色 | 2026-03-12：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Tech] Blood Sea - field render path is specialized"` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS | 回归防护：`tests/tech/UITests.cpp` 锁定 `RenderSystem.cpp` 存在 `BloodSeaFieldComponent` 专用渲染分支、半径驱动与 `DrawRing` 路径 | 已完成代码验证与回归验证，关闭 |
| BUG-20260312-003 | 2026-03-12 | 手测 | P2 | Closed | `Blood Sea` 持续时间不明确，建议在 Buff 栏增加以血海技能为图标的 Buff，并显示血海当前能力 | 1) 施放 `Blood Sea`；2) 观察现有 UI 是否能直接给出剩余时间与当前场体能力；3) 对比 Buff 栏与现有顶部/资源提示；4) 发现难以从现有 UI 读取完整状态 | 使用 Demon Blade 的 `Blood Sea` 技能时触发 | 技能状态可读性；Buff 栏一致性；战斗决策反馈 | 实际存在玩家 Buff 栏：`UISystem::DrawBuffs()` 会读取 `ActiveEffectsComponent` 并在血条上方绘制 buff 图标；之前问题在于 `Blood Sea` 运行时没有给玩家挂载独立 active buff，也没有唯一图标/tooltip 文案，导致信息被挤到资源 HUD 文本里 | `src/game/components/Buff.hpp`; `src/game/data/BuffRegistry.cpp`; `src/game/systems/skill/behaviors/BloodSea.cpp`; `src/engine/render/UIRenderer.cpp`; `src/game/systems/ui/PlayerHUD.cpp`; `src/engine/resource/BuffAssetRegistry.hpp`; `tests/unit/BuffTests.cpp`; `tests/unit/SkillBehaviorGuardTests.cpp`; `tests/tech/UITests.cpp` | 新增 `BuffType::BloodSea` 与 `buff_xuehai` 图标；`BloodSea` 施放/持续期间会给玩家刷新 `blood_sea_active` buff，并把当前形态/能力写入运行时 tooltip 文案；`UIRenderer` 允许优先显示 `BuffEffect.name/description`；同时移除资源 HUD 上冗余的 `Blood Sea ...` 细节串，回归真实 buff 区域表达 | 2026-03-12：`python scripts/gen_asset_registries.py` PASS；`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] BuffRegistry - Lookup Logic,[Tech] PlayerHUD - Render Logic,[Tech] BuffUI - Blood Sea uses buff lane icon and runtime tooltip overrides"` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] SkillBehaviorGuard - Deep dive cadence and miasma refresh"` PASS；`ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS | 回归防护：1) `tests/unit/BuffTests.cpp` 锁定 `BuffType::BloodSea` 图标映射有效；2) `tests/unit/SkillBehaviorGuardTests.cpp` 锁定玩家会获得 `blood_sea_active`；3) `tests/tech/UITests.cpp` 锁定 Blood Sea 不再挤占资源 HUD，且 BuffRegistry / BloodSea / UIRenderer 三处路径保持连通 | 已完成代码验证与回归验证，关闭 |
| BUG-20260312-004 | 2026-03-12 | 手测 | P2 | Closed | 剑意图标上方显示 `???`，含义不明，疑似职业专精相关占位文本或未正确本地化 | 1) 进入战斗并激活相关职业专精/资源 UI；2) 观察剑意图标上方文本；3) 出现 `???`；4) 对比不同专精或状态下是否变化 | 显示职业专精相关资源提示时触发；见用户附件图1 | 资源 UI 可读性；本地化；状态文本正确性 | 图标上方的阈值文本来自 `SwordIntentWidget` 的中文状态串（如 `危险线`、`血海临界`），但该控件使用默认 `DrawText/MeasureText` 路径而不是 UI 字体，导致缺少中文字形时显示成 `???` | `src/game/systems/ui/SwordIntentWidget.cpp`; `tests/tech/UITests.cpp` | 将图标上方三行文本全部切到 `UISystem::GetFont()` + `UISystem::DrawTextUI` 路径，保持居中测量逻辑不变，同时使用已加载 UI 字体渲染中文状态文案 | 2026-03-12：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Tech] SkillUI - SwordIntentWidget status text uses UI font rendering"` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS | 回归防护：`tests/tech/UITests.cpp` 锁定 `SwordIntentWidget.cpp` 不再使用原始 `DrawText` 绘制状态行，必须走 UI 字体路径 | 已完成代码验证与回归验证，关闭 |
| BUG-20260312-005 | 2026-03-12 | 手测 | P2 | Closed | 技能专精节点标题文字与标签底色过近，文本可读性差，难以辨认节点信息 | 1) 打开技能专精界面；2) 查看带标签的专精节点；3) 观察标题文字与标签/底色对比度；4) 在用户附件图2所示场景中难以看清文字 | 技能专精界面显示带标签节点时触发；深色背景与近似色标签组合下更明显 | UI 可读性；专精构筑体验；无障碍对比度 | `UISkillTalentTree` 的 tooltip 标题直接使用 `masteryTheme.highlight`，标签文字直接使用 `badge.outline`，在 Demon Blade 等近色主题下文字和标签/头部色相太接近，缺少对比保护 | `src/game/systems/ui/UISkillTalentTree.cpp`; `src/game/systems/ui/BladeMasteryUITheme.cpp`; `tests/tech/UITests.cpp` | 保持现有主题语言不变，只把 tooltip 标题和标签文字切到单独的高对比可读文字色；色相信息继续保留在标签填充、描边和头部装饰条上 | 2026-03-12：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Tech] SkillUI - tooltip title and badges use dedicated readable text treatment"` PASS；`ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` PASS | 回归防护：`tests/tech/UITests.cpp` 锁定 tooltip 标题/标签存在专门的可读文字处理，不再直接复用主题强调色作为正文色 | 已完成代码验证与回归验证，关闭 |
| BUG-20260313-001 | 2026-03-13 | Code Review | P1 | Closed | Duration Preview Wiring: character panel and tooltip do not reflect quantitative duration scaling | 1) Open Character Panel; 2) Hover a skill with duration scaling (e.g. Blood Sea); 3) Observe tooltip display lines | Tooltip rendering without `DurationScale` metadata in `TalentNode` or missing `duration_scale` in character stats | Character Panel, Skill Tooltip, Talent System | `TalentNode` JSON parser missed `display_lines` metadata; `UISkillHub` and `UISkillTalentTree` didn't pass `SkillDisplayPreview` payload to tooltip renderer | `src/game/systems/ui/UISkillHub.cpp`; `src/game/systems/ui/UISkillTalentTree.cpp`; `src/game/data/TalentRegistry.cpp`; `src/game/systems/ui/CharacterPanel.cpp` | 1) Updated `TalentNode::fromJson` to parse `display_lines`; 2) `CharacterPanel` now shows `duration_scale` row; 3) Tooltip renderer now consumes `SkillDisplayPreview` metadata for quantitative line rendering | 2026-03-14: `[Tech] CharacterPanel - duration scale row is visible` PASS; `[Unit] TalentNode JSON - display_lines parse quantitative tooltip metadata` PASS; `[Tech] SkillUI - specialization tooltip renders quantitative lines` PASS | `tests/tech/CharacterPanelTests.cpp`; `tests/unit/TalentRegistryTests.cpp` | Integrated with Heavenly Sword restoration |
| BUG-20260313-002 | 2026-03-13 | Code Review | P1 | Verified | Blade resource 命中追踪清理疑似混用绝对时间与累计时间，导致过期条目可能无法按预期清理 | 1) 为 `BladeResourceComponent.hit_tracking` 写入旧的 `last_gain_time`；2) 调用 `BladeResourceService::Update()`；3) 观察旧条目未被稳定清理 | `TryGainBloodthirstOnLowLifeMeleeHit()` 以绝对时间写入，而 `CleanupTracking()` 用 `time_since_last_gain` 做 now 值时触发 | 血怒/资源追踪正确性；内存与状态稳定性；回归分析可预测性 | `tracking.last_gain_time` 使用绝对时间，`BladeResourceService::Update()` 却把累计计时 `time_since_last_gain` 作为 `CleanupTracking()` 的 now 参数，导致同一张 `hit_tracking` 表按两套时间语义读写 | `src/game/systems/skill/BladeResourceService.cpp`; `src/game/systems/skill/SkillSystem.cpp`; `tests/unit/BladeMasteryTests.cpp` | 在 `BladeResourceService::Update()` 中统一使用 `const float current_time = static_cast<float>(GetTime())`，并把该绝对时间传给 `CleanupTracking()`；新增 `[Unit] Blade Resource Service - expires stale hit tracking` 回归测试，固定 stale entry 会被 `Update()` 清理 | 2026-03-13：新增单测先红后绿；`cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Unit] Blade Resource Service - expires stale hit tracking"` PASS；`ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure` PASS（9/9） | 回归防护：`tests/unit/BladeMasteryTests.cpp` 新增 stale hit-tracking 过期移除测试，并保留整组 unit suite 复测 | 本轮 correctness 修复已闭环，可继续推进 BUG-20260313-003 |
| BUG-20260313-003 | 2026-03-13 | Code Review | P2 | Verified | Blade mastery 存档恢复后缺少完整归一化，可能保留与当前专精不兼容的 resource/signature 运行态 | 1) 构造 `blade_mastery` 与 `blade_resource`/`blade_signature_skill` 不一致的快照；2) 调用 `SaveManager::restoreFromSnapshot()`；3) 观察恢复后仍保留不兼容状态 | 存档含 Blade mastery 相关字段，且资源字段存在时触发 | 存档兼容性；运行态一致性；后续数据演进安全性 | 双重根因：1) `restoreFromSnapshot()` 在有 `blade_resource` 时跳过 `RefreshPlayerState()`，导致共享 mastery 归一化逻辑未完整复用；2) 归一化改为无条件后，恢复路径仍未把存档等级回灌到 `PlayerLevel`，而相关 roundtrip 测试也缺少 `Astrolabe`/BladeMasteryRegistry` 前提，导致合法 Heavenly Sword 快照被按“未解锁/无 profile”回退 | `src/engine/persistence/SaveManager.cpp`; `src/game/systems/skill/BladeMasteryService.cpp`; `tests/unit/SystemMechanics.cpp` | 1) `restoreFromSnapshot()` 现在无条件调用 `BladeMasteryService::RefreshPlayerState()`；2) `createSnapshot()` 从 `PlayerStats`/`PlayerLevel` 写入 `header.level`，恢复时回灌 `PlayerLevel`，让归一化使用真实存档等级；3) 新增不兼容快照归一化回归测试，并把 Blade mastery roundtrip 测试补齐 `Astrolabe`、等级和 `BladeMasteryRegistry` 前提 | 2026-03-14：`cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Unit] SaveManager - Blade mastery snapshot roundtrip" -s && ./bin/NoMoreDayTests.exe --test-case="[Unit] SaveManager - normalizes incompatible blade mastery runtime state" -s` PASS；`ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure` PASS（9/9） | 回归防护：`tests/unit/SystemMechanics.cpp` 保留合法 Blade mastery roundtrip 与不兼容快照归一化两条用例，分别锁定“合法快照不被错误清空”和“脏运行态会被规范化” | correctness 修复已闭环，可继续推进 Heavenly Sword 闭环 |
| BUG-20260312-006 | 2026-03-12 | 手测 | P2 | Closed | `Blood Sea` buff 的倒计时圆环只在持续时间最后约 0.5 秒才明显开始减少，视觉上像“后段才掉圈” | 1) 施放 `Blood Sea` 并观察血条上方的 buff 图标；2) 对比图标圆环倒计时与 tooltip 剩余时间；3) 观察到 tooltip 持续正常递减，但圆环长时间接近满值，最后约 0.5 秒才明显收缩 | 使用 `blood_sea_active` 玩家 buff 表达 `Blood Sea` 持续状态时触发 | Buff 倒计时一致性；战斗状态可读性；后续同类 active buff 设计复盘 | `BloodSea::SyncBloodSeaActiveBuff()` 在持续期间每帧同步 buff 时，把 shrinking 的 `field.duration` 同时写进 `BuffEffect.duration` 和 `BuffEffect.remaining`；UI 圆环用 `remaining / duration` 计算，导致比值长期接近 1，仅在结束前才快速下降 | `src/game/systems/skill/behaviors/BloodSea.cpp`; `src/game/components/Buff.hpp`; `src/game/systems/ui/UISystem.cpp`; `src/engine/render/UIRenderer.cpp`; `tests/unit/SkillBehaviorGuardTests.cpp` | 调整 `blood_sea_active` 的同步策略：同一片 `Blood Sea` 持续期间仅更新 `remaining` 与动态 tooltip 文案，保留首次施放时的总 `duration`；仅在明确重新施放/刷新为新场时才重置总时长 | 2026-03-12：`./build.bat && ./bin/NoMoreDayTests.exe --test-case="[Unit] SkillBehaviorGuard - Deep dive cadence and miasma refresh"` PASS（103 assertions） | 回归防护：`tests/unit/SkillBehaviorGuardTests.cpp` 锁定 `blood_sea_active` 在真实帧序列下保留初始总时长，同时 `remaining < duration` 正常递减；后续同类 active buff 应复用该检查思路 | 这是由“运行时剩余时间”与“总时长”混写引起的典型 UI 计时 bug，已闭环并适合作为后续 Buff 倒计时问题的复盘样例 |
| BUG-20260314-001 | 2026-03-14 | Code Review | P2 | Closed | `UISkillHub` 只对技能 `10` 做签名技锁定，导致技能 `11/12` 在可用技能网格里仍显示为可拖拽/可专精 | 1) 选择任一 Blade Ascendant 专精；2) 打开 `UISkillHub` 可用技能网格；3) 观察另一条专精的签名技仍未显示“未解锁”且可开始拖拽 | 进入技能专精界面并浏览签名技能 10/11/12 时触发 | 技能专精 UI 一致性；设计完成度；签名技产品化体验 | `UISkillHub` 在可用技能网格里把 `signatureLocked` 硬编码为 `(id == 10)`，没有复用 `BladeMasteryService::IsSignatureSkillUnlocked(...)` 对全部 Blade Ascendant 签名技的共享门控 | `src/game/systems/ui/UISkillHub.cpp`; `tests/tech/UITests.cpp` | 已修复：将 hub 网格的签名技判定扩展为 `id == 10 || id == 11 || id == 12`，统一继续走 `BladeMasteryService::IsSignatureSkillUnlocked(...)` 共享门控；新增 tech 回归测试锁定不再只特判技能 10 | 2026-03-14：`cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Tech] SkillUI - mastery hub locks all Blade Ascendant signature skills consistently,[Tech] SkillUI - mastery hub exposes Heavenly Sword attunement controls"` PASS（2/2） | 回归防护：`tests/tech/UITests.cpp` 新增 `[Tech] SkillUI - mastery hub locks all Blade Ascendant signature skills consistently`，锁定 UI 不再只特判技能 10，且继续复用共享签名技解锁判断 | 该设计/产品一致性问题已闭环，可继续推进 BUG-20260314-002 |
| BUG-20260314-002 | 2026-03-14 | Code Review | P2 | Closed | `Seven Star Slash` 仍可能把尸体/已死亡敌人纳入初始目标或剑痕爆裂目标，且 individual slash 循环也会命中非聚焦的邻近尸体 | 1) 让一个带 `EnemyTag` 的目标进入死亡态；2) 在其附近保留一个活体敌人；3) 施放技能 10；4) 观察尸体仍可能被初始获取、剑痕爆裂或 individual slash 命中 | `SevenStarSlash` 仅按 `EnemyTag + Position + CombatStats` 收集候选目标时触发 | 技能 10 目标正确性；死亡实体语义；多段斩击稳定性 | 根因是同一个 `GatherTargets()` 同时服务“初始选目标/聚焦目标”和“后续多段重取目标/剑痕爆裂”，原修复方案仅在初始和爆裂阶段引入 `GatherLiveTargets()`，但未解决 individual slash 循环中对邻近非聚焦尸体的冗余命中 | `src/game/systems/skill/behaviors/SevenStarSlash.cpp`; `tests/functional/SkillBehaviors.cpp` | 已修复：1) `IsLiveTargetCandidate()` 补齐 `CombatStats::health <= 0` 校验；2) slash-loop 改用 `GatherLiveTargets()` 过滤邻近尸体；3) 保留 slash-loop 结尾的 rescue logic（不带死亡过滤）以确保已锁定的聚焦目标即使中途死亡也能完成整段斩击动画 | 2026-03-14：`cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Functional] Skill - Seven Star Slash*"` PASS（7/7，含新增爆裂与尸体过滤专项） | 回归防护：`tests/functional/SkillBehaviors.cpp` 新增 `[Functional] Skill - Seven Star Slash - Scar Explosion filters dead targets` 等用例，锁定“尸体不被命中”且“聚焦死亡目标仍保持动画完成性” | 修复已全面闭环，支持无 `sevenFocus` 场景下的邻近尸体隔离 |
| BUG-20260314-003 | 2026-03-14 | Code Review | P2 | Closed | 存档恢复仍会保留 Blade resource 的瞬时战斗计时器/窗口状态，导致短暂 proc 与衰减计时跨存档延续 | 1) 在战斗中让 Blade resource 进入临时窗口状态（如 restart window / crit feedback / decay timer）；2) 立即保存并恢复；3) 观察短暂计时器、反馈状态或 hit-tracking 仍被保留 | 角色存在 `BladeResourceComponent` 且直接做快照/恢复时触发 | 存档正确性；短时战斗状态一致性；后续数据演进安全性 | `SaveManager` 直接 round-trip `BladeResourceComponent`，而 `RefreshPlayerState()` 只校正耐久资源配置，不会清理 `time_since_last_gain`、`restart_window_*`、`crit_bonus_feedback_timer`、`decay_tick_timer` 等瞬时运行态；测试还暴露出这类回归必须带上 `BladeMasteryRegistry` 和存档等级前提，避免把 profile/解锁归一化噪音混进用例 | `src/engine/persistence/SaveManager.cpp`; `src/game/components/SkillDefs.hpp`; `src/game/systems/skill/BladeMasteryService.cpp`; `tests/unit/SystemMechanics.cpp` | 已修复：在 restore 路径对 `BladeResourceComponent` 的瞬时字段做归一化后再 `SyncLegacySwordIntent` / `RefreshPlayerState()`；重置 `time_since_last_gain`、`last_crit_bonus_time`、`crit_bonus_feedback_timer`、`restart_window_timer`、`restart_window_ready`、`decay_tick_timer`，并清空 `hit_tracking`；新增 save/load 回归测试锁定短时窗口不会跨存档延续且恢复后无法消费旧 restart window | 2026-03-14：`cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Unit] SaveManager - normalizes transient blade resource restore state"` PASS（1/1）；随后 `cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Tech] SkillUI - mastery hub locks all Blade Ascendant signature skills consistently,[Tech] SkillUI - mastery hub exposes Heavenly Sword attunement controls" && ./bin/NoMoreDayTests.exe --test-case="[Functional] Skill - Seven Star Slash*" && ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure` PASS（tech 2/2；Seven Star 功能切片 5/5；unit 9/9） | 回归防护：`tests/unit/SystemMechanics.cpp` 新增 `[Unit] SaveManager - normalizes transient blade resource restore state`，锁定耐久资源状态保留、瞬时计时器归零、且恢复后不能消费旧 restart window | 该中等存档正确性问题已闭环，三个 branch-finalization 前的 medium follow-up 已全部完成 |
| BUG-20260314-004 | 2026-03-14 | Code Review | P2 | Verified | `FreshBloodReturn` 功能测试会把 Blade mastery 调试解锁全局开关留在开启状态，导致后续测试顺序相关 | 1) 运行 `./bin/NoMoreDayTests.exe --test-case="[Functional] Skill - Blood Sea - Node 1211 FreshBloodReturn"`；2) 随后在同进程运行任意依赖默认锁定状态的 Blade mastery 测试；3) 观察后续测试在强制解锁前提下运行 | `tests/functional/SkillBehaviors.cpp` 中测试直接调用 `BladeMasteryService::SetDebugUnlockOverrideEnabled(true)` 且未恢复时触发 | 功能/单元测试隔离性；本地与 CI 验证可信度；push 前信心 | `BladeMasteryService` 的调试解锁标志是进程级全局状态；`FreshBloodReturn` 修改了该状态却未恢复，且 `TestSetupScope` 目前不会重置该标志，导致同进程后续用例可能被污染 | `tests/functional/SkillBehaviors.cpp`; `src/game/systems/skill/BladeMasteryService.cpp` | 已修复：在 `FreshBloodReturn` 用例内保存进入前的调试解锁状态，用局部 guard 在栈展开时自动恢复，并新增结束态断言锁定 `IsDebugUnlockOverrideEnabled()` 会回到原值 | 2026-03-14：先在 `tests/functional/SkillBehaviors.cpp` 添加结束态断言并复现失败（`CHECK( true == false )`）；随后执行 `cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Functional] Skill - Blood Sea - Node 1211 FreshBloodReturn"` PASS（1/1）；再执行 `./bin/NoMoreDayTests.exe --test-case="[Functional] Skill - Blood Sea*,[Unit] Blade Mastery*,[Unit] Blade Resource Service*"` PASS（12/12） | 回归防护：`tests/functional/SkillBehaviors.cpp` 现已锁定该功能测试结束后 `IsDebugUnlockOverrideEnabled()` 恢复为进入用例前的值，且 guard 能覆盖 `REQUIRE` 早退路径 | 这是典型测试污染问题，已记录并通过用例内回归断言闭环，后续同类全局开关测试应复用“保存旧值 + guard 恢复”模式 |
| BUG-20260315-001 | 2026-03-15 | Code Review | P1 | Verified | Blood Sea 专精预览元数据与运行时数值漂移，且时长预览百分比按小数语义解析，导致单测失败并误导 Tooltip/预览 | 1) 运行 `ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure`；2) 观察 `tests/unit/SkillRegistryMasteryTreeTests.cpp` 对 Blood Sea 节点预览数据断言失败；3) 检查 `SkillDisplayPreview` 专项用例与 UI 百分比格式；4) 对照 `BloodSea.cpp` 的实际运行时公式 | Blood Sea 关键专精节点由行为代码直接计算效果、但 `mastery_skill_trees.json` 仍保留过时描述/数值；同时 preview/UI 路径把百分比值当成 `0.1 = 10%` 语义时触发 | 专精树 Tooltip；技能时长预览；专精数据可信度；后续 Demon Blade 数值回归 | 双重根因：1) Blood Sea 节点 `1200/1219` 的 JSON 描述与预览元数据未跟随运行时公式更新，仍缺少能表达真实数值的 `display_lines`；2) `SkillDisplayPreviewService` 与 `UISkillTalentTree` 对百分比值采用小数语义，而 `StatsSystem`/资产数据使用整百分比语义（`10.0 = 10%`） | `assets/data/mastery_skill_trees.json`; `src/game/systems/skill/SkillDisplayPreviewService.cpp`; `src/game/systems/ui/UISkillTalentTree.cpp`; `tests/unit/SkillRegistryMasteryTreeTests.cpp`; `tests/unit/SkillDisplayPreviewTests.cpp`; `tests/tech/UITests.cpp` | 已修复：1) `SkillDisplayPreviewService` 改为按整百分比语义计算专精时长预览；2) `UISkillTalentTree` 自动定量 tooltip 百分比格式改为直接渲染整百分比值；3) Blood Sea 节点 `1200/1219` 移除误导性 `stat_modifiers`，补齐与运行时一致的 `display_lines` 和描述文案 | 2026-03-15：先执行 `cmake --build build --config RelWithDebInfo --target NoMoreDayTests && ./bin/NoMoreDayTests.exe --test-case="[Unit] SkillDisplayPreview - duration preview with specialization modifiers,[Unit] SkillDisplayPreview - duration preview with multiplicative specialization modifiers,[Unit] Skill Registry - Blood Sea specialization preview data matches runtime-authored values,[Tech] SkillUI - quantitative tooltip percent formatting uses whole-percent values"` PASS（4/4，23 assertions）；随后执行 `ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure`，当前仅剩 `tests/unit/GPUABIGovernanceTest.cpp:56` 的运行时预算断言失败（`2535.28ms > 2000.0ms`），与本条修复无关 | 回归防护：`tests/unit/SkillDisplayPreviewTests.cpp` 锁定时长预览的整百分比加算与乘算语义；`tests/unit/SkillRegistryMasteryTreeTests.cpp` 锁定 Blood Sea `1200/1219` 的预览数据与运行时一致；`tests/tech/UITests.cpp` 以源码回归方式锁定 UI 自动百分比格式不再把整百分比值重复乘以 `100` | 本条已完成目标范围内的红绿验证；更大范围的 unit suite 仍受独立 GPU ABI 运行时预算问题阻塞，技能 10/11 其它历史数据漂移可在后续独立审计 |
| BUG-20260315-002 | 2026-03-15 | 手测 | P1 | Resolved | 掉落物有些时候不显示，或者会闪烁 | 1) 进入战斗场景并击杀怪物；2) 观察地面掉落物表现；3) 发现部分掉落物不可见或出现高频闪烁 | 掉落物较多或特定视角/渲染负载下更易触发 | 掉落物可见性、拾取反馈 | 2026-03-15 根因核查结论：1) `GPULootSystem::SyncDroppedItems` 会在收集阶段先按 `m_maxInstances` 截断实例，再用截断后的 `m_syncedInstanceCount` 调用 `EnsureCapacity`，因此超容量时不会触发扩容，可直接解释“部分掉落不显示”；2) 现有 `GPUUtils::DispatchCompute` 已在每次 compute dispatch 后插入 `glMemoryBarrier(GL_SHADER_STORAGE_BARRIER_BIT)`，渲染前也有全量 barrier，因此原登记中的“阶段间缺少 barrier”假设与当前代码不符；3) “闪烁”现象的第二根因尚未在静态代码中确认，需继续围绕 force-directed/可见实例运行时链路复现定位 | `src/engine/render/GPULootSystem.cpp`; `src/engine/render/GPUUtils.cpp`; `src/engine/render/GPULootSystem.hpp`; `src/engine/render/RenderSystem.cpp`; `src/game/systems/item/DropSystem.cpp`; `tests/unit/GPULootSystemTests.cpp` | 已修复掉落溢出路径：1) `SyncDroppedItems` 改为先统计真实需求量，再 `EnsureCapacity(required)`，最后上传全部有效实例；2) 新增 `GetMaxInstancesForTest()` 与 `DebugSnapshot`，记录 `required/synced/maxInstances/visible` 供测试与后续闪烁排查；3) 保留现有渲染链路，避免在未确认第二根因前误改 barrier/dispatch 顺序 | 2026-03-15：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] GPULootSystem*,[Unit] InventoryDragSwap*,[Tech] InventoryUI - button text uses shared emoji fallback path,[Unit] AISystem -*,[Unit] EnemyAIBehaviors -*"` PASS（9/9，31 assertions），其中 `GPULootSystem` 超容量扩容回归通过 | 回归防护：1) `tests/unit/GPULootSystemTests.cpp` 锁定 overflow 时必须先扩容再同步；2) `DebugSnapshot` 保留 `required/synced/maxInstances/visible` 运行时观测点；3) 后续补高掉落量手测，确认“无丢失、无闪烁” | 掉落丢失根因已修复并验证；“闪烁”仍缺稳定复现证据，需在高掉落量场景继续手测后再考虑转 `Verified/Closed` |
| BUG-20260315-003 | 2026-03-15 | 手测 | P1 | Verified | 背包界面无法通过拖拽来装备或替换装备，包括背包扩展栏 | 1) 打开背包界面；2) 尝试拖拽物品到装备槽或背包扩展槽；3) 尝试在装备槽间拖拽替换；4) 观察操作是否生效 | 任意职业与物品配置下触发 | 交互体验、构筑效率 | 2026-03-15 根因核查结论：1) 满容量交换死锁属实，但装备槽与背包扩展槽路径并不完全相同：`equipItem` 会在清空拖拽物源槽前先调用 `unequipItem`，而 `unequipItem` 依赖现有 `null` 槽位才能回包；`equipBag` 则在移除拖拽背包前先调用 `unequipBag`，其容量检查也会把拖拽中的背包仍计入占用，因此满包/近满包时交换会失败；2) 从装备槽或背包扩展槽拖回背包指定格 `i` 时，`UIInventory` 忽略目标索引并走通用 `unequipItem/unequipBag` 落包路径，导致物品进入首个可用槽而非用户指定位置 | `src/game/systems/ui/UIInventory.cpp`; `src/game/systems/item/InventorySystem.cpp`; `src/game/systems/item/InventorySystem.hpp`; `tests/unit/InventoryDragSwapTests.cpp` | 已修复：1) 新增显式事务 API，先释放源槽或预留目标槽，再执行装备/扩展槽交换；2) 为“装备 -> 背包指定格”“背包扩展 -> 背包指定格”补专用落点 API，避免退回首个空槽；3) `UIInventory` 改走新 swap/move 语义，覆盖满包替换与指定格落点 | 2026-03-15：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] GPULootSystem*,[Unit] InventoryDragSwap*,[Tech] InventoryUI - button text uses shared emoji fallback path,[Unit] AISystem -*,[Unit] EnemyAIBehaviors -*"` PASS（9/9，31 assertions），其中 `InventoryDragSwap` 回归通过 | 回归防护：`tests/unit/InventoryDragSwapTests.cpp` 覆盖满包拖拽换装、指定背包格落点与扩展槽替换，锁定 swap 语义不再依赖中间空槽 | 已完成代码验证；仓库级 `ci` 复跑当前仍被独立 Heavenly Sword/SkillContract 用例阻塞，与本问题无关 |
| BUG-20260315-004 | 2026-03-15 | 手测 | P3 | Verified | "🧹 整理背包"的 emoji 显示为 ? | 1) 打开背包界面；2) 查看底部“整理背包”按钮文案 | 背包底部按钮使用 emoji 文案，且按钮文本未走 emoji fallback 渲染路径时触发 | UI 美观度、本地化一致性 | 2026-03-15 根因核查结论：1) 按钮文案确实直接使用了 `🧹`；2) 主 UI 字体 atlas 未包含扫帚表情 `U+1F9F9`，但 `UISystem` 实际上已单独加载包含该 glyph 的 emoji fallback 字体；3) 当前按钮渲染走 `UIRenderer::DrawButton` 内部的主字体 `DrawTextEx` 路径，没有复用 `DrawTextUI` 的 emoji fallback 逻辑，这才是该按钮显示 `?` 的直接原因 | `src/game/systems/ui/UIInventory.cpp`; `src/game/systems/ui/UISystem.cpp`; `src/engine/render/UIRenderer.cpp`; `tests/tech/UITests.cpp` | 已修复：1) 为按钮文字补统一的 emoji-aware 测量逻辑；2) `DrawButton` 改为使用 `DrawTextUI`/emoji fallback 路径完成阴影与正文绘制；3) 保证测量与绘制走同一套 fallback 逻辑，避免按钮居中偏移 | 2026-03-15：`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] GPULootSystem*,[Unit] InventoryDragSwap*,[Tech] InventoryUI - button text uses shared emoji fallback path,[Unit] AISystem -*,[Unit] EnemyAIBehaviors -*"` PASS（9/9，31 assertions），其中 `[Tech] InventoryUI - button text uses shared emoji fallback path` 通过 | 回归防护：`tests/tech/UITests.cpp` 锁定 `UIRenderer::DrawButton` 必须复用共享 emoji fallback 测量/绘制路径，不再直接走主字体 `DrawTextEx` | 已完成代码验证；仓库级 `ci` 复跑当前仍被独立 Heavenly Sword/SkillContract 用例阻塞，与本问题无关 |
| BUG-20260315-005 | 2026-03-15 | 手测 | P2 | Verified | 部分怪物未触发索敌机制，即使距离很近 | 1) 靠近场景中的怪物；2) 观察怪物是否进入战斗状态/开始追踪；3) 发现部分怪物无反应 | 场景中存在多个怪物且距离玩家较近时触发 | 战斗交互、AI 行为正确性 | 2026-03-15 根因核查结论：1) `DormantTag` 并非完全没有唤醒路径，`GameplayState` 每帧会调用 `EnemySpawnSystem::updateDormantEntities` 回收休眠实体；实际问题是唤醒距离常量 `1650~1800` 大于 `AISystem` 的 `1600` 休眠阈值，实体被唤醒后仍可能很快再次休眠，形成“被回收但未真正恢复索敌”的假唤醒；2) 当前索敌顺序是先检查 `activationRange`，再用 `detectionRange` 搜索目标，文档原先“activationRange 晚于 detectionRange 检查”的表述不准确；但两套门控叠加后，实际仇恨半径会被压到两者较小值，配置不一致时会让索敌范围比设计预期更小 | `src/game/systems/ai/AISystem.cpp`; `src/game/states/GameplayState.cpp`; `src/game/systems/world/EnemySpawnSystem.cpp`; `src/game/components/Common.hpp`; `src/game/components/EnemyComponent.hpp`; `tests/unit/AISystemDormancyAggroTests.cpp` | 已修复：1) 将唤醒环带压回休眠阈值内，确保被回收实体不会在下一次 `AISystem::update` 立刻重新休眠；2) 将 `EnemyStateComponent` 默认 `activationRange` 与 `detectionRange` 同步，`spawnEnemy` 生成路径也显式同步，避免默认配置漂移；3) `AISystem` 在 `IDLE/PATROL` 中改为以 `max(activationRange, detectionRange)` 作为单一有效索敌半径，并用同一半径完成门控与目标搜索，消除双门控压缩 | 2026-03-15：`powershell.exe -Command "& 'C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\amd64\MSBuild.exe' 'build\tests\NoMoreDayTests.vcxproj' /p:Configuration=RelWithDebInfo /p:Platform=x64 /m:1 /verbosity:minimal" && ./bin/NoMoreDayTests.exe --test-case="[Unit] AISystem -*,[Unit] EnemyAIBehaviors -*"` PASS（4/4，13 assertions）；`./build.bat` PASS；`./bin/NoMoreDayTests.exe --test-case="[Unit] GPULootSystem*,[Unit] InventoryDragSwap*,[Tech] InventoryUI - button text uses shared emoji fallback path,[Unit] AISystem -*,[Unit] EnemyAIBehaviors -*"` PASS（9/9，31 assertions） | 回归防护：1) 新增 `[Unit] AISystem - awakened dormant enemies stay active on the next AI tick`，锁定假唤醒不会回退成即刻再休眠；2) 新增 `[Unit] AISystem - enemies aggro within detection range even when activation range is smaller`，锁定有效仇恨半径不再被 `activationRange/detectionRange` 压成更小值；3) 保留既有 `EnemyAIBehaviors` 单测切片复跑，确认相邻 AI 行为路径未被破坏 | 已完成定向修复与最终回归；仓库级 `ci` 复跑当前仍被独立 Heavenly Sword/SkillContract 用例阻塞，与本问题无关 |





