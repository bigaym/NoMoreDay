# Blade Ascendant VFX Infrastructure (V3) - Implementation Plan

> **Track ID**: `blade_ascendant_vfx_infrastructure_20260222`  
> **依赖设计**: `设计文档/特效和UI/BladeAscendant_VFX_Design_v3.md`  
> **状态**: [ ] Not Started

---

## 阶段总览

| 阶段 | 名称 | 核心产出 | 状态 |
|---|---|---|---|
| **Phase 1** | 现状审计 | 入口/约束/差异清单 | [ ] |
| **Phase 2** | 事件契约落地 | `SkillVfxEvent` 扩展与兼容策略 | [ ] |
| **Phase 3** | 配方系统骨架 | 配置加载 + 模板选择 + 动作调度 | [ ] |
| **Phase 4** | Tier/回退对齐 | 回退开关与预算钩子 | [ ] |

---

## Phase 1: 现状审计 (5 Tasks)

- [ ] Task 1.1: 对齐 V3 设计中 §6/§7/§8/§9 的实现落点（文件级 mapping）。
- [ ] Task 1.2: 盘点现有 VFX 能力：`GPUParticleSystem` / `GPUTrailRenderer` / `GPUSkillEffectSystem` / `DistortionPass`。
- [ ] Task 1.3: 明确禁止项落实点：FBO0、全局 SSBO binding 已满、VFXPass 禁止 Compute。
- [ ] Task 1.4: 盘点可复用资产与脚本：`scripts/gen_blade_vfx_assets.py`、`scripts/gen_vfx_textures.py`、`assets/shaders/vfx/*`。
- [ ] Task 1.5: 写出“VFX 配方 JSON schema 草案”（仅 schema，不要求实现本阶段完成）。

## Phase 2: 事件契约落地 (6 Tasks)

- [ ] Task 2.1: 扩展 `SkillVfxEventType`：增加 `TransmuterSwitch`、`KeystoneActivate`。
- [ ] Task 2.2: 扩展 `SkillVfxEvent`：补齐 `elementType`、`resistDebuffType` 字段并定义默认值策略。
- [ ] Task 2.3: 更新 `SkillSystem` 的事件生产路径，确保字段填充与 `effectiveTags/nodeRoleMask` 一致。
- [ ] Task 2.4: 更新 `GPUSkillEffectSystem` 的事件消费路径（兼容旧事件）。
- [ ] Task 2.5: 为新字段补齐最小单元测试/断言（至少保证枚举值与默认值不漂移）。
- [ ] Task 2.6: 编译验证：`build.bat notest`。

## Phase 3: 配方系统骨架 (8 Tasks)

- [ ] Task 3.1: 定义 `SkillVfxRecipe` 与选择器优先级规则（支持通配/覆盖）。
- [ ] Task 3.2: 实现配方配置加载（建议：`assets/data/vfx/blade_ascendant_v3.json`）。
- [ ] Task 3.3: 将 `GPUSkillEffectSystem` 的“硬编码 switch”拆分为：解析事件→选择配方→执行动作。
- [ ] Task 3.4: 动作适配层：粒子（`GPUParticleSystem`）/尾迹（`GPUTrailRenderer`）/扭曲（`DistortionPass`）/叠加（SSBO6）。
- [ ] Task 3.5: 在 `assets/shaders/vfx/` 增加 `vfx_element_switch.glslinc`（先支持色板选择即可）。
- [ ] Task 3.6: 预留抗性削弱 overlay 的绘制入口（不新增 Pass，优先复用 `VFXPass` 或现有 overlay 模式）。
- [ ] Task 3.7: 选取 1 个技能做 end-to-end 冒烟：CastStart/CastImpact/TriggerProc/EmpoweredConsume 事件均可出图。
- [ ] Task 3.8: `ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure`（如新增单测）。

## Phase 4: Tier/回退对齐 (3 Tasks)

- [ ] Task 4.1: 将 V3 §9 回退顺序映射到 `RenderConfig` 与 VFX 配方（粒子倍率、distortion 开关、采样 stride 等）。
- [ ] Task 4.2: 对齐 `QualityTierManager` 的 AutoDegrade：至少能触发“DisableDistortion”和“降低细节”。
- [ ] Task 4.3: 记录 Tier 验收检查清单到本 Track `validation.md`（后续 Gate track 复用）。

---
_Generated by Feature Planner Engine._

