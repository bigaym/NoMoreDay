# Validation — combat_boss_framework_20260225

## Verification Evidence
_(To be filled during implementation)_
- Boss prototype flow → PENDING
- Counter window precision → PENDING
- `build.bat` + `ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure` → PENDING
