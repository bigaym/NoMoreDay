# Implementation Plan: Loot Text GPU Instancing & Layout Optimization

**Track ID:** loot-text-gpu_20260131
**Spec:** [spec.md](./spec.md)
**Created:** 2026-01-31
**Status:** [ ] Not Started

## Overview

本计划旨在将战利品标签的渲染完全卸载至 GPU，并解决海量掉落时的视觉拥挤问题。我们将复用 `PopupRenderer` 的 SSBO 架构，但升级其支持 Raylib 的动态字符 UV 系统。

## Phase 1: GPU Glyph Pipeline Foundation

建立基础的实例化字符渲染架构。

### Tasks

- [x] Task 1.1: 在 `src/engine/render/GPUData.hpp` 中定义 `GPUGlyphInstance` 结构体（位置、UV、颜色）。
- [x] Task 1.2: 在 `src/engine/render/RenderConstants.hpp` 中增加文字渲染专用的 SSBO 绑定点（如 `Binding::SSBO_TEXT_GLYPH`）。
- [x] Task 1.3: 编写 `assets/shaders/glyph.vert` 和 `assets/shaders/glyph.frag` 着色器，支持从 SSBO 读取字符 UV 并在对应位置进行采样。
- [x] Task 1.4: 在 `RenderSystem` 中初始化 `s_glyphInstanceBuffer` 和相关的 Shader 资源。

### Verification

- [x] 编译通过，Shader 成功加载。

## Phase 2: Dynamic Glyph Batching Implementation

实现将中文字符串拆解为 GPU 实例的逻辑。

### Tasks

- [x] Task 2.1: 开发 `LootTextBatcher` 工具类，负责遍历字符串，通过 `GetGlyphIndex` 获取 Raylib Font 中的 UV 信息。
- [x] Task 2.2: 重构 `RenderSystem` 的掉落物收集逻辑，将 `DrawTextEx` 调用替换为推入 `s_glyphBuffer`。
- [x] Task 2.3: 实现一次性 `glDrawArraysInstanced` 提交逻辑，确保所有掉落物文字仅用 1 个 Draw Call。

### Verification

- [x] 屏幕上的掉落物文字显示正确（包括中文、颜色和缩放）。
- [x] 通过 RenderDoc 或日志确认，Draw Call 数量显著下降。

## Phase 3: Anti-overlap Layout Algorithm

实现标签的排斥与堆叠算法。

### Tasks

- [x] Task 3.1: 在渲染收集阶段，引入 AABB 碰撞检测。
- [x] Task 3.2: 实现简单的贪心布局算法：按 Y 轴排序标签，检测重叠并进行纵向位移补偿（垂直堆叠）。
- [x] Task 3.3: 限制布局计算的频率（例如 5 帧/次）以平衡性能。

### Verification

- [x] 当多个物品掉落在同一点时，标签能够上下排开，互不遮挡。

## Phase 4: Integration & Hardening

系统集成与最终性能压测。

### Tasks

- [x] Task 4.1: 对接 `LootFilterResultComponent` 的所有属性（隐藏、强调色、放大）。
- [x] Task 4.2: 移除遗留的 `s_textQueue` 和相关的旧版测量代码。
- [x] Task 4.3: 进行万级掉落物极端压力测试，验证 400+ FPS 目标。

### Final Verification

- [x] 所有验收标准均已达成。
- [x] 性能数据符合预期 (< 0.3ms)。
- [x] 代码符合项目规范，无显存泄漏。

---
_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
