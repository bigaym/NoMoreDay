# 2D PBR Material Pipeline 规格说明书

> **Track ID**: `v4_pbr_material_pipeline_20260219`  
> **设计参考**: [`GPU_Rendering_System_V4.md` §5](../../../设计文档/特效和UI/GPU_Rendering_System_V4.md)  
> **主控规格书**: [`rendering_engine_v4_master_spec.md`](../../specs/rendering_engine_v4_master_spec.md)  
> **状态**: 📋 Pending

---

## 1. 概述

将 V3 Material 2.0（Normal/Roughness/Specular 基础字段）升级为完整的 2D PBR 材质标准，引入四层贴图管线（Albedo/Normal/Mask/Detail）和 BRDF-Lite 光照模型，实现"同光异材"的真实层次感。

## 2. 贴图规范

| 贴图层 | 格式 | 通道说明 | 美术要求 |
|--------|------|----------|----------|
| **Albedo** | RGBA8 | 基础颜色（去除烘焙光影） | 纯色彩信息，无明暗 |
| **Normal** | RGB8 (线性) | 切线空间法线（Z 朝屏外） | 手绘或 3D 预渲染导出 |
| **Mask** | RGBA8 | R=Roughness, G=Metallic, B=Height/AO, A=Emission | 单张复用四通道 |
| **Detail** (可选) | RG8 | 细节法线缩放 | Ultra 档专属 |

## 3. Data Model

### 3.1 GPUMaterialDataV3 (128 bytes)

```cpp
struct alignas(16) GPUMaterialDataV3 {
    glm::vec4 baseColor;              // 16
    glm::vec4 emissiveAndIntensity;   // 16
    glm::vec4 pbrParams;              // 16  (roughness, metallic, ao, heightBias)
    glm::vec4 textureSlots;           // 16  (albedoIdx, normalIdx, maskIdx, detailIdx)
    glm::vec4 fresnelControl;         // 16  (f0Override, rimSuppress, roughnessBias, reserved)
    glm::vec4 uvParams;               // 16  (tilingScale.xy, scrollSpeed.xy)
    glm::vec4 reserved[2];            // 32
};
static_assert(sizeof(GPUMaterialDataV3) == 128);
```

### 3.2 Schema 升级

- `material_schema_version: 2 → 3`
- V2→V3 JSON 自动映射默认值（`fresnelControl` = {0.04, 0.3, 0.1, 0}, `uvParams` = {1,1,0,0}）
- V2 JSON 不崩溃，仅日志 warning

## 4. BRDF-Lite 光照模型

### 4.1 核心组件
- **NDF**: GGX (Trowbridge-Reitz)
- **Geometry**: Schlick-GGX 简化
- **Fresnel**: Schlick + 2D 边缘抑制（`mix(1.0, 0.3, step(0.7, 1-NdotV))`）

### 4.2 2D 专用修正
| 问题 | 解决方案 |
|------|----------|
| Sprite 边缘菲涅尔高光 | Fresnel 抑制因子 |
| 法线方向过于统一 | `roughness = roughness * 0.8 + 0.1` bias |
| 无真实背面 | 禁用 backface 光照计算 |

### 4.3 Sprite-Based AO
- 离线烘焙到 Mask.B 通道
- 运行时：`finalAO = min(bakedAO, ssaoSample) * aoIntensity`

## 5. 美术资产工具链

```
原始 Sprite (Albedo only)
    ↓
离线工具链 (Python/C++ CLI)
├── Height Map 生成 (AI 辅助)
├── Normal Map 从 Height 导出
├── AO 从 Height 烘焙
├── Roughness/Metallic 手绘/AI
└── Mask 四通道打包
    ↓
Texture2DArray (Albedo层 + Normal层 + Mask层)
    ↓
MaterialManager → SSBO<GPUMaterialDataV3>
```

## 6. 与 V3 的关系

- V3 `GPUMaterialDataV2` (128B) → V4 `GPUMaterialDataV3` (128B, 字段重组)
- `normalMapSlot` → `textureSlots.y`（位置不变，语义拓宽）
- 新增 `fresnelControl`, `uvParams` 控制
- ABI 版本 3 → 4（随本 Track 递增）

## 7. 验收标准

- [ ] 至少 3 类 Sprite（玩家/怪物/场景物体）有 Normal+Mask 贴图
- [ ] "同光异材"层次感可辨（金属 vs 布料 vs 石材）
- [ ] V2 材质 JSON 自动映射默认值，不崩溃
- [ ] Material Schema V3 热重载正常
- [ ] ScenePass 新增开销 ≤ 0.2ms（Normal/Mask 采样）

---
_Generated by Feature Planner Engine._
