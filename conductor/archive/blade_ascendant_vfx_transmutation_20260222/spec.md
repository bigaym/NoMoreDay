# Blade Ascendant VFX Transmutation (Element Variants) - Specification

> **Track ID**: `blade_ascendant_vfx_transmutation_20260222`  
> **输入设计**: `设计文档/特效和UI/BladeAscendant_VFX_Design_v3.md` §3, §5, §6, §8  
> **依赖**: `blade_ascendant_vfx_base_forms_20260222`  
> **状态**: 📋 Pending

---

## 1. 目标

在 Base Form 基线之上，为 9 技能补齐“元素变转”视觉差异：

- 事件侧：`TransmuterSwitch` 与 `elementType` 驱动模板切换；
- 渲染侧：尽量**不新增 SSBO 结构体**，优先通过 `GPUSkillEffect.flags` 编码 elementType 并在 shader 中选择色板/纹理；
- 资源侧：补齐 V3 设计 §11 中新增的元素纹理占位（程序化生成优先）。

---

## 2. 约束

- 不新增全局 SSBO binding（0-15 已满）。
- Shader 复用优先：统一 `vfx_element_switch.glslinc` 作为 element palette switch。
- Low/Medium 下元素差异可降级，但必须保留“技能判读优先级”。

---

## 3. 验收标准 (DoD)

- [ ] 9 技能的元素变转切换正确（至少覆盖 Fire/Cold/Lightning/Void 中的 2 种变体/技能，按 V3 设计要求）。
- [ ] 资源生成脚本可重复运行（无手工依赖），并记录在 validation。
- [ ] 不违反 RenderGraph/FBO0/SSBO binding 约束。

---
_Generated by Feature Planner Engine._

