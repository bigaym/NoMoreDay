# Radiance Cascades Global Illumination 规格说明书

> **Track ID**: `v5_radiance_cascades_gi_20260219`  
> **设计参考**: [`GPU_Rendering_System_V5.md` §4](../../../设计文档/特效和UI/GPU_Rendering_System_V5.md)  
> **主控规格书**: [`rendering_engine_v5_master_spec.md`](../../specs/rendering_engine_v5_master_spec.md)  
> **状态**: 📋 Pending

---

## 1. 概述

实现无噪点、实时、全动态的 2D 全局光照，利用辐射级联的半影假设将 GI 计算分解为多个级联层，每层负责不同距离范围的光照传输。

## 2. 级联架构

| 级联层 | 空间分辨率 | 探针间距 | 射线数/探针（Balanced） | 射线长度 | 角度覆盖 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| **L0** | 屏幕 1:1 | 1px | 4 | 0-4px | 4×90° |
| **L1** | 1/2 | 2px | 4 | 4-8px | 4×90° |
| **L2** | 1/4 | 4px | 8 | 8-16px | 8×45° |
| **L3** | 1/8 | 8px | 8 | 16-32px | 8×45° |
| **L4** | 1/16 | 16px | 12 | 32-64px | 12×30° |
| **L5** | 1/32 | 32px | 12 | 64-128px+ | 12×30° |

> 说明：默认采用 `Balanced` 角分辨率增长策略。V5-A 先锁定该策略，V5-B 再评估更激进 profile（例如 L4/L5=16）是否值得。

## 3. Emissive Buffer

发光体来源：

| 来源 | 数据通道 | 更新频率 |
|------|----------|----------|
| 场景灯光（火把/水晶） | V4 LightManager 投影 | 每帧 |
| 自发光材质（岩浆/魔法纹路） | Material Mask.A (Emission) | 静态 |
| VFX 粒子（火焰/闪电） | 粒子 emissive 参数 | 每帧 |
| 技能特效 | VFX Sequencer 驱动 | 事件触发 |

**格式**: RGBA16F, 屏幕分辨率

## 4. RenderGraph 集成

```
Scene → LightCulling → Shadow → Lighting
    → [OccluderExtract] → [JFA] → [RadianceCascades] → [GI Composite]
    → Volumetric → VFX → UIWorld → PostProcess → Distortion → Composite
```

## 5. 时域稳定性

```glsl
vec3 finalGI = mix(currentFrameGI, previousFrameGI, temporalWeight);
// temporalWeight = 0.85~0.95
```

- 相机移动时自动降低 `temporalWeight`
- 静止时提高 `temporalWeight`
- 场景光源突变时重置 temporal history

## 6. ABI 新增

### RadianceCascadeConfig (32 bytes)

```cpp
struct RadianceCascadeConfig {
    uint32_t numLevels;         // 4  级联层数 (4 or 6)
    uint32_t raysPerProbe;      // 4  L0 基准射线数（每级射线由 profile 派生）
    float    baseInterval;      // 4  基础射线长度
    float    temporalWeight;    // 4  时域混合权重
    uint32_t halfResolution;    // 4  是否 half-res
    uint32_t sdfUpdateInterval; // 4  SDF 更新间隔
    float    giIntensity;       // 4  GI 强度乘数
    uint32_t reserved;          // 4
};
static_assert(sizeof(RadianceCascadeConfig) == 32);
```

## 7. Holographic RC 探索（V5-B 阶段）

V5-B 阶段评估单次全局计算方案：
- 若性能更优，可替代标准 RC
- 若无法在 OGL 4.3 高效实现，保持标准 RC 为主方案

## 8. Feature Flag

```cpp
render.gi.enabled = true/false
render.gi.cascadeLevels = 4/6
render.gi.halfResolution = true/false
render.gi.temporalWeight = 0.9
render.gi.sdfUpdateInterval = 1/2/4
```

## 9. 验收标准

### 9.1 V5-A 阶段（4-cascade half-res 原型）
- [ ] 4 级联 half-res GI 在 **≥3 场景类型**（洞穴/城镇/森林）下呈现可辨间接光照（明暗对比 ≥ 10%）
- [ ] 时域混合在相机以普通游戏速度移动时无可察觉拖影
- [ ] GI 与 V4 直接光照叠加无过曝/过暗（最终亮度在 [0.0, 1.2] HDR 范围内）
- [ ] GI 开关 `render.gi.enabled` 切换无黑帧
- [ ] Ultra 档常规场景 ≥ **180 FPS**（4-cascade half-res, 4070S@1080p）

### 9.2 V5-B 阶段（6-cascade full-res 完整体）
- [ ] **6 级联 full-res 在洞穴场景呈现颜色溢出**（color bleeding：相邻面颜色溢出亮度变化 ≥ 10%，截图可辨）
- [ ] **6 级联单独耗时 ≤ 2.5ms** @1080p (4070S)
- [ ] Holographic RC 可行性评估结论已文档化
- [ ] High 档常规场景 ≥ **270 FPS**（4-cascade half-res 维持不降）

---
_Generated by Feature Planner Engine._
