# Implementation Plan: UI Button Visual Polish

**Track ID:** ui-button-polish_20260131
**Spec:** [spec.md](./spec.md)
**Created:** 2026-01-31
**Status:** [ ] Not Started

## Overview

在 `UIRenderer` 中建立统一的按钮渲染标准，并逐一重构现有 UI 组件的渲染逻辑。

## Phase 1: Foundation & Resource Loading

确保资源可用并建立底层渲染函数。

### Tasks

- [x] Task 1.1: 在 `UISystem::Initialize` 中添加新增按钮资源的加载代码。
- [x] Task 1.2: 在 `UIRenderer` 中实现 `DrawButton` 静态方法，支持贴图渲染、文字自动居中、Hover 状态颜色叠加。
- [x] Task 1.3: 验证资源加载是否成功。

### Verification

- [x] 通过日志或调试渲染确认贴图已加载。
- [x] 编写简单的测试代码验证 `DrawButton` 的布局准确性。

## Phase 2: Main Menu Refactor

替换游戏入口界面的按钮视觉。

### Tasks

- [x] Task 2.1: 修改 `MainMenuState::DrawButton`，调用新的渲染接口并应用 `Button_Menu` 纹理。
- [x] Task 2.2: 调整主菜单按钮的尺寸和文字间距，适配 3:1 的纹理比例。

### Verification

- [x] 运行游戏进入主菜单，确认按钮视觉已更新。

## Phase 3: In-Game Panel Refactor (Stash & Inventory)

替换核心玩法界面的按钮视觉。

### Tasks

- [x] Task 3.1: 重构 `UIStash` 中的“整理”、“存入”以及搜索栏/关闭按钮。应用冰蓝系列纹理。
- [x] Task 3.2: 重构 `UIInventory` 中的功能按钮（如背包切换等，如果存在）。
- [x] Task 3.3: 重构 `UICharacter` 或其他已发现的硬编码矩形按钮。

### Verification

- [x] 在城镇中打开仓库，确认按钮美化效果及文字对齐。

## Final Verification

- [ ] 所有界面按钮风格统一。
- [ ] 2K/1080p 缩放测试。
- [ ] 性能检查（由于 UI 贴图已预加载，对 FPS 的影响应可忽略不计）。

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
