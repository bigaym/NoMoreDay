track combat_summon_strategy_v1_20260225 closeout: SummonSystem split to Lifecycle/AI/CombatBridge; SummonComponent string->archetype_id; summon attribution tuple (owner/summon/source_skill) added to combat events; spirit sword + melee orbit migrated to bridge with inherit modes and proc budget runtime; tests added in SummonStrategyTests; verification passed via build.bat and ctest --test-dir build -C RelWithDebInfo -L ci --output-on-failure (two rounds including pre-commit rerun).
