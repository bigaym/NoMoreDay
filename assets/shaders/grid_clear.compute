#version 430
layout(local_size_x = 256) in;
layout(std430, binding = 2) buffer CellCountBuffer { uint cellCounts[]; };
uniform int numCells;
void main() {
    uint id = gl_GlobalInvocationID.x;
    if (id < numCells) cellCounts[id] = 0;
}
