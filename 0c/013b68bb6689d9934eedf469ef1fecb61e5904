Skill spec prerequisite required_points integrated end-to-end (JSON parse/model, unlock checks, UI lock/availability). Added dependency-dot rendering on links by required_points. Added unit tests for threshold and OR semantics. Build workflow optimized: stale-process cleanup, novalidate/nostaleclean flags, notest now target-level, reduced redundant source glob headers. Verification: build.bat pass; ctest -L unit pass.
