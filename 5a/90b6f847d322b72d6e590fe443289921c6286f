track v4_advanced_lighting_20260219 closeout: implemented GlobalHeightField chunk-incremental pipeline + HeightShadowPass integration; shadowMapIndex clustered coupling via ShadowPrepare->LightManager writeback and pass-order contract update (Scene->Shadow->LightCulling->Lighting->HeightShadow). Verification: build.bat PASS; build.bat analyze PASS; ctest --test-dir build -C RelWithDebInfo -L unit PASS; ctest --test-dir build -C RelWithDebInfo -L integration PASS; ctest --test-dir build -C Release -L performance FAIL non-blocking (ParticleTrail Scenario4 dispatchOverheadMs=0.246931 > 0.2). Archived track folder to conductor/archive and synced tracks docs.
