# P0-2 Baseline Snapshot (2026-03-03)

## Test status baseline

- Capture date (UTC): `2026-03-03`
- Branch/commit: `feat/four-pillars-phase0-kickoff` / `b1ad821f54211fd58f853d9688943fd75253f99f`
- Command set:
  - `./build.bat check`
  - `ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure`
  - `ctest --test-dir build -C RelWithDebInfo -L integration --output-on-failure`
  - `ctest --test-dir build -C Release -L performance --output-on-failure`
- Results:
  - check: `PASS` (see `../P0-3/results.md`)
  - unit: `PASS` (`1/1`, `nmd.tests.unit`, 2.56s)
  - integration: `PASS` (`1/1`, `nmd.tests.integration`, 2.71s)
  - performance: `PASS` (`1/1`, `nmd.tests.performance`, 12.62s)

## Test map and gap signal

- Source: `test-map.md` and `test-map.json` in this folder.
- Heuristic gap signals:
  - `ui`: 6 test files
  - `item`: 8 test files
  - `progression`: 0 test files
- Stronger domains by test-file footprint: `render`, `combat`, `skill`.

## Performance baseline status

- Gate budget for initiative: `median P95 <= baseline + 5%`.
- Deterministic P95 baseline values are pending until canonical perf runner/scenario list is finalized in Phase 0.
- Current coarse indicator: performance-labeled CTest suite passes on this commit.

## Hardware/profile snapshot (host)

- OS build: `Windows-10-10.0.19045-SP0`
- CPU: `AMD64 Family 25 Model 97 Stepping 2, AuthenticAMD`
- Logical cores: `16`
- GPU: `<collect in deterministic perf-runner step>`
- Driver version: `<collect in deterministic perf-runner step>`
- RAM: `<collect in deterministic perf-runner step>`

## Notes

- This snapshot is suitable for P0-2 kickoff and test-gap prioritization.
- Deterministic performance protocol evidence will be appended once runner/profile commands are locked.
