# P0-2 Baseline Snapshot (2026-03-03)

## Test status baseline

- Capture date (UTC): `2026-03-03`
- Branch/commit: `feat/four-pillars-phase0-kickoff` / `32af4f6135f635d4da3ad097510d60d770a520b4`
- Command set:
  - `python scripts/perf/capture_host_profile.py`
  - `python scripts/perf/run_deterministic_p95_baseline.py`
  - `ctest --test-dir build -C RelWithDebInfo -L unit --output-on-failure`
  - `ctest --test-dir build -C RelWithDebInfo -L integration --output-on-failure`
  - `ctest --test-dir build -C Release -L performance --output-on-failure`
- Results:
  - host profile capture: `PASS` (`host-profile.json`)
  - deterministic perf runner: `PASS` (`perf-baseline-run.json`)
  - unit: `PASS` (`1/1`, `nmd.tests.unit`, 1.51s)
  - integration: `PASS` (`1/1`, `nmd.tests.integration`, 2.35s)
  - performance: `PASS` (`1/1`, `nmd.tests.performance`, 13.10s)

## Test map and gap signal

- Source: `test-map.md` and `test-map.json` in this folder.
- Heuristic gap signals:
  - `ui`: 6 test files
  - `item`: 8 test files
  - `progression`: 0 test files
- Stronger domains by test-file footprint: `render`, `combat`, `skill`.

## Performance baseline status

- Gate budget for initiative: `median P95 <= baseline + 5%`.
- Deterministic protocol evidence captured:
  - warmup runs: `1`
  - measured runs: `5`
  - fixed scenario command: `./bin/NoMoreDayTests.exe --test-case="[Performance] MaterialVFX - MaterialSwap+Distortion Stress P95"`
  - measured values (ms): `0.006`, `0.006`, `0.006`, `0.007`, `0.007`
  - median metric (ms): `0.006`
- Metric caveat: test output does not emit explicit P95 numeric values; runner uses `LOG_BENCHMARK` P99 output as a deterministic tail-latency proxy (`p95_proxy_p99_ms`).
- Evidence files:
  - `perf-baseline-run.json`
- Raw per-run stdout/stderr logs are also generated to `perf-run-logs/` locally (gitignored).

## Hardware/profile snapshot (host)

- OS build: `Windows-10-10.0.19045-SP0`
- CPU: `AMD Ryzen 7 7700 8-Core Processor`
- Logical cores: `16`
- GPU: `GameViewer Virtual Display Adapter`
- Driver version: `17.13.25.697`
- RAM: `31862 MiB`
- Source: `host-profile.json`

## Notes

- This snapshot is suitable for P0-2 kickoff and test-gap prioritization.
- Deterministic baseline runner and host profile capture are now scripted under `scripts/perf/`.
